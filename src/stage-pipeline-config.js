/**
 * Stage Pipeline Config — single config table driving all advance logic.
 *
 * completion-handler queries this table instead of hardcoding stage names.
 */

const STAGE_PIPELINE_CONFIG = Object.freeze([
  {
    stageId: 'spec',
    role: 'pm',
    tier: 'pm',
    timeout: 1800,
    reviewGate: false,
    cycleTarget: null,
    cycleCondition: null,
    advanceCondition: 'passed',
    entryCriteria: '有明确的用户需求或变更请求',
    exitCriteria: 'PRD 完成且覆盖所有 FR/AC',
    roleHint: '下一阶段是 spec-review-gate，建议派 SA/架构 agent 做需求评审',
  },
  {
    stageId: 'spec-review-gate',
    role: 'architecture',
    tier: 'arch',
    timeout: 1200,
    reviewGate: true,
    cycleTarget: 'spec',
    cycleCondition: 'failed',
    advanceCondition: 'passed',
    entryCriteria: 'PRD 已提交且格式完整',
    exitCriteria: '架构师确认需求无歧义、可实现',
    roleHint: '下一阶段是 test-case-authoring，建议派 audit agent 编写测试用例',
  },
  {
    stageId: 'test-case-authoring',
    role: 'review',
    tier: 'audit',
    timeout: 1200,
    reviewGate: false,
    cycleTarget: null,
    cycleCondition: null,
    advanceCondition: 'passed',
    entryCriteria: 'spec-review-gate 通过',
    exitCriteria: '测试用例覆盖全部 FR/AC',
    roleHint: '下一阶段是 ux-acceptance-authoring，建议派 UX agent',
  },
  {
    stageId: 'ux-acceptance-authoring',
    role: 'ux',
    tier: 'ux',
    timeout: 1200,
    reviewGate: false,
    cycleTarget: null,
    cycleCondition: null,
    advanceCondition: 'passed',
    entryCriteria: 'test-case-authoring 完成',
    exitCriteria: 'UX 验收标准文档产出',
    roleHint: '下一阶段是 commercial-acceptance-authoring，建议派 PM agent',
  },
  {
    stageId: 'commercial-acceptance-authoring',
    role: 'pm',
    tier: 'pm',
    timeout: 1200,
    reviewGate: false,
    cycleTarget: null,
    cycleCondition: null,
    advanceCondition: 'passed',
    entryCriteria: 'UX 验收标准已就位',
    exitCriteria: '商业验收标准文档产出',
    roleHint: '下一阶段是 ux-interaction-design，建议派 UX agent 做交互设计',
  },
  {
    stageId: 'ux-interaction-design',
    role: 'ux',
    tier: 'ux',
    timeout: 1800,
    reviewGate: false,
    cycleTarget: null,
    cycleCondition: null,
    advanceCondition: 'passed',
    entryCriteria: '验收标准已定义',
    exitCriteria: '交互设计稿完成，含组件规格',
    roleHint: '下一阶段是 architecture-design，建议派 SA agent 做架构设计',
  },
  {
    stageId: 'architecture-design',
    role: 'architecture',
    tier: 'arch',
    timeout: 3600,
    reviewGate: false,
    cycleTarget: null,
    cycleCondition: null,
    advanceCondition: 'passed',
    entryCriteria: '交互设计和需求规格都已就位',
    exitCriteria: '架构设计文档+ADR 产出',
    roleHint: '下一阶段是 contract，建议派 SA agent 编写契约',
  },
  {
    stageId: 'contract',
    role: 'architecture',
    tier: 'arch',
    timeout: 3600,
    reviewGate: false,
    cycleTarget: null,
    cycleCondition: null,
    advanceCondition: 'passed',
    entryCriteria: '架构设计已完成',
    exitCriteria: '接口契约文档产出，覆盖全部模块边界',
    roleHint: '下一阶段是 contract-review-gate，建议派 audit agent 审查契约',
  },
  {
    stageId: 'contract-review-gate',
    role: 'review',
    tier: 'audit',
    timeout: 1200,
    reviewGate: true,
    cycleTarget: 'contract',
    cycleCondition: 'failed',
    advanceCondition: 'passed',
    entryCriteria: '契约文档已提交',
    exitCriteria: '审计确认契约完整、无矛盾',
    roleHint: '下一阶段是 implement，建议派 coding agent 执行实现',
  },
  {
    stageId: 'implement',
    role: 'dev',
    tier: 'T1',
    timeout: 1200,
    reviewGate: false,
    cycleTarget: null,
    cycleCondition: null,
    advanceCondition: 'passed',
    entryCriteria: 'contract-review-gate 通过',
    exitCriteria: '代码实现完成，通过本地测试',
    roleHint: '下一阶段是 review，建议派 audit agent 做代码审计',
  },
  {
    stageId: 'review',
    role: 'audit',
    tier: 'audit',
    timeout: 1200,
    reviewGate: true,
    cycleTarget: 'fix',
    cycleCondition: 'failed',
    advanceCondition: 'passed',
    entryCriteria: '实现代码已提交',
    exitCriteria: '代码审计通过，0 P0/P1 findings',
    roleHint: '下一阶段是 smoke-test，建议派 audit agent 做冒烟测试',
  },
  {
    stageId: 'fix',
    role: 'dev',
    tier: 'T1',
    timeout: 1200,
    reviewGate: false,
    cycleTarget: 'review',
    cycleCondition: 'passed',
    advanceCondition: null,
    entryCriteria: 'review 产出 findings 需修复',
    exitCriteria: '修复完成，findings 已逐条解决',
    roleHint: '修复完成后回到 review，建议派 audit agent 重新审计',
  },
  {
    stageId: 'smoke-test',
    role: 'audit',
    tier: 'audit',
    timeout: 1200,
    reviewGate: false,
    cycleTarget: null,
    cycleCondition: null,
    advanceCondition: 'passed',
    entryCriteria: 'review 通过',
    exitCriteria: '冒烟测试全部通过',
    roleHint: '下一阶段是 ux-acceptance，建议派 UX agent 做验收',
  },
  {
    stageId: 'ux-acceptance',
    role: 'ux',
    tier: 'ux',
    timeout: 1800,
    reviewGate: false,
    cycleTarget: null,
    cycleCondition: null,
    advanceCondition: 'passed',
    entryCriteria: 'smoke-test 通过',
    exitCriteria: 'UX 验收通过，交互符合设计稿',
    roleHint: '下一阶段是 pm-commercial-review，建议派 PM agent 做商业评审',
  },
  {
    stageId: 'pm-commercial-review',
    role: 'pm',
    tier: 'pm',
    timeout: 1200,
    reviewGate: true,
    cycleTarget: 'implement',
    cycleCondition: 'failed',
    advanceCondition: 'passed',
    entryCriteria: 'UX 验收通过',
    exitCriteria: '商业评审确认产品满足商业目标',
    roleHint: '下一阶段是 regression，建议派 coding agent 跑回归测试',
  },
  {
    stageId: 'regression',
    role: 'dev',
    tier: 'T1',
    timeout: 1200,
    reviewGate: false,
    cycleTarget: null,
    cycleCondition: null,
    advanceCondition: 'passed',
    entryCriteria: 'pm-commercial-review 通过',
    exitCriteria: '回归测试全部通过，无新 failures',
    roleHint: '下一阶段是 publish-generalization-gate，建议派 SA agent 做发布泛化评审',
  },
  {
    stageId: 'publish-generalization-gate',
    role: 'architecture',
    tier: 'arch',
    timeout: 1200,
    reviewGate: true,
    cycleTarget: 'implement',
    cycleCondition: 'failed',
    advanceCondition: 'passed',
    entryCriteria: 'regression 通过',
    exitCriteria: '代码泛化程度满足发布标准',
    roleHint: '下一阶段是 deploy，建议派 coding agent 执行部署',
  },
  {
    stageId: 'deploy',
    role: 'dev',
    tier: 'T1',
    timeout: 600,
    reviewGate: false,
    cycleTarget: null,
    cycleCondition: null,
    advanceCondition: 'passed',
    entryCriteria: 'publish-generalization-gate 通过',
    exitCriteria: '部署成功，服务可用',
    roleHint: '下一阶段是 verify，建议派 audit agent 做部署验证',
  },
  {
    stageId: 'verify',
    role: 'audit',
    tier: 'audit',
    timeout: 600,
    reviewGate: false,
    cycleTarget: null,
    cycleCondition: null,
    advanceCondition: 'passed',
    entryCriteria: '部署成功',
    exitCriteria: '验证服务正常运行',
    roleHint: '下一阶段是 readme，建议派 PM agent 更新文档',
  },
  {
    stageId: 'readme',
    role: 'pm',
    tier: 'pm',
    timeout: 600,
    reviewGate: false,
    cycleTarget: null,
    cycleCondition: null,
    advanceCondition: 'passed',
    entryCriteria: 'verify 通过',
    exitCriteria: 'README 和变更日志已更新',
    roleHint: '下一阶段是 post-release-validation，建议派 audit agent 做发布后验证',
  },
  {
    stageId: 'post-release-validation',
    role: 'audit',
    tier: 'audit',
    timeout: 1200,
    reviewGate: false,
    cycleTarget: null,
    cycleCondition: null,
    advanceCondition: 'passed',
    entryCriteria: 'readme 完成',
    exitCriteria: '发布后验证通过，线上无异常',
    roleHint: '下一阶段是 clean-install-verification，建议派 coding agent 做全新安装验证',
  },
  {
    stageId: 'clean-install-verification',
    role: 'dev',
    tier: 'T1',
    timeout: 1200,
    reviewGate: false,
    cycleTarget: null,
    cycleCondition: null,
    advanceCondition: 'passed',
    entryCriteria: 'post-release-validation 通过',
    exitCriteria: '全新环境安装验证通过',
    roleHint: '下一阶段是 ledger，建议派 coding agent 写入交付账本',
  },
  {
    stageId: 'ledger',
    role: 'dev',
    tier: 'T3',
    timeout: 600,
    reviewGate: false,
    cycleTarget: null,
    cycleCondition: null,
    advanceCondition: 'passed',
    entryCriteria: 'clean-install-verification 通过',
    exitCriteria: '交付记录写入账本',
    roleHint: '流水线全部阶段完成，无后续阶段',
  },
]);

const STAGE_CONFIG_MAP = Object.freeze(
  Object.fromEntries(STAGE_PIPELINE_CONFIG.map((entry) => [entry.stageId, entry]))
);

function getStageConfig(stageId) {
  return STAGE_CONFIG_MAP[stageId] || null;
}

function getCycleTarget(stageId, status) {
  const config = STAGE_CONFIG_MAP[stageId];
  if (!config?.cycleTarget || !config.cycleCondition) return null;
  if (config.cycleCondition === status) return config.cycleTarget;
  return null;
}

function getRoleHint(stageId) {
  const config = STAGE_CONFIG_MAP[stageId];
  return config?.roleHint || '';
}

function getEntryCriteria(stageId) {
  const config = STAGE_CONFIG_MAP[stageId];
  return config?.entryCriteria || '';
}

function getExitCriteria(stageId) {
  const config = STAGE_CONFIG_MAP[stageId];
  return config?.exitCriteria || '';
}

export { STAGE_PIPELINE_CONFIG, STAGE_CONFIG_MAP, getStageConfig, getCycleTarget, getRoleHint, getEntryCriteria, getExitCriteria };
