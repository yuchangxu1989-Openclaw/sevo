# KIVO需求规格说明书

# KIVO - 产品需求规格说明书

OpenClaw(sa-01 子Agent)| 2026-05-25

---

## 目录

1. 产品愿景与定位
1. 目标用户画像
1. 痛点
1. 原始需求
1. 用户体验流
1. 核心概念
1. 功能需求(FR)
- 域 A:知识提取
- 域 B:知识存储与检索
- 域 C:知识迭代
- 域 D:调研管理
- 域 E:意图理解增强
- 域 F:规则订阅与分发
- 域 G:知识图谱与洞察
- 域 H:系统词典
- 域 I:OpenClaw 集成层
- 域 K:知识管线编排
- 域 L:分析中间产物
- 域 M:知识域目标声明
- 域 N:知识治理
- 域 W:知识工作台
- 域 X:访问控制与可观测性
- 域 Z:开箱即用与商用就绪
- 域 P:领域知识库(LLM Wiki)
1. 非功能需求(NFR)
1. 概念架构
1. UX 验收标准
1. 与 Self-Evolving Harness 其他模块的边界
1. 约束与假设

---

## 1. 产品愿景与定位

KIVO(Knowledge Iteration & Vibe Orchestration)= Agent 知识平台 + 个人知识工作台。

产品定位:用户的认知进化伙伴。系统定位:Agent 知识平台。

KIVO 的用户交互体验目标是远超市面上现有的知识库和 AI 笔记应用(Obsidian、Notion、Mem、Reflect 等)。前端交互体验是 KIVO 作为平台的核心竞争力,不是附属品。具体体现在:交互式知识图谱探索(力导向布局、节点着色、缩放聚焦)、调研任务管理(任务队列、产出查看、采纳标记)、文档导入流水线(分段提取、逐条确认、原文溯源)。全站采用浅色主题(白色背景 + 黑色文字)。

KIVO 覆盖知识的完整生命周期--从多源提取、结构化存储、语义检索、冲突解决、调研管理到图谱洞察。Agent 通过 API 消费知识,用户通过 Web 工作台录入、探索、研究和治理知识资产。工作台是独立产品对象,围绕四个核心入口构建:意图知识库、领域知识库(LLM Wiki)、知识图谱、知识搜索。

KIVO 解决的核心问题:

- Agent 的知识散落在对话、记忆文件、配置和临时笔记里,没有结构、没有版本、没有主动补盲机制。
- 用户的知识散落在文档、笔记、浏览器书签和脑子里,缺乏结构化沉淀、关联发现和缺口洞察。
- 知识的录入、分析、沉淀、探索、调研形成的完整闭环,当前没有一个系统能端到端覆盖。

设计硬约束:

- KIVO Web 是非场景化的知识基础组件管理台,不是学习 APP。Web 端职责是把材料、参考资料、Wiki 页面和真正能指导决策/行动的知识分门别类管好。教材概念、公式、定理、例题、原文摘录属于参考资料或 Wiki 编译素材,不等同于 Knowledge Entry,不得直接混入知识库主表。不做:学习进度、学习管线可视化、"正在阅读/已学会/掌握度"等场景化语言、健康度仪表盘。质量门禁、向量化等内部机制不暴露给用户。学习进度和学习交互属于场景化应用(如 exam-sprint),不属于 KIVO。
- 知识先结构化再存储,不存原始文本堆。
- 知识冲突必须自动解决,不允许新旧矛盾共存。治理 cron 负责检测和解决冲突,不暴露为用户手动操作。
- 调研由用户通过对话主动发起,Agent 执行并产出报告。
- 规则分发按需订阅,不全量灌注。
- 代码不硬编码路径、凭据和环境细节,保持工程质量;但产品层面 KIVO 只服务于 OpenClaw 宿主环境,不承诺支持其他平台。
- 引擎层和工作台层独立演进,工作台是独立产品对象,不是引擎的附属页面。

### 成功指标

- 知识检索命中率 ≥ 85%(Agent 查询时返回相关知识条目的比例)。度量方法:基于人工标注测试集,检索结果 Top-K 中包含标注相关条目的查询占比。
- 缺口检测覆盖率 ≥ 70%(已知盲区中被自动发现的比例)。
- 知识冲突自动解决率 100%(冲突发生后由治理 cron 自动解决,不允许悬而未决)。
- 规则分发到达率 ≥ 95%(订阅者收到规则更新的比例)。
- 首次知识旅程完成时间 ≤ 10 分钟(空库用户从安装到看到系统价值)。

---

## 2. 目标用户画像

### 2.1 个人知识工作者

拥有大量文档、笔记、研究资料,需要一个系统帮助整理、关联和发现知识盲区。

- 场景:在电脑前整理一批文档(论文、教材、笔记、课件),希望系统自动解析参考资料并建立关联;通过对话下达调研任务,Agent 执行调研并产出报告,用户标记采纳后系统按知识准入门禁拆解入库。
- 设备:桌面浏览器(主力)、平板浏览器(辅助阅读)。
- 关注点:录入体验流畅、图谱探索直观、深度研究闭环完整。

### 2.2 Solo Founder / 独立产品操盘者

用 Agent 推进产品研发、运营和决策。

- 场景:日常通过对话与 Agent 协作,产生大量决策、方法论和经验教训;需要 Agent 记住这些,不在同一个坑上反复踩;定期发起调研任务,Agent 执行并产出报告。
- 设备:桌面浏览器。
- 关注点:意图知识自动沉淀、规则分发到位、调研闭环。

### 2.3 Agent 开发者

构建和维护 Agent 系统。

- 场景:需要统一的知识管理接口,让不同 Agent 共享知识而不互相污染;需要规则分发机制,确保系统规则变更能及时传达到所有相关 Agent。
- 设备:桌面浏览器 + CLI。
- 关注点:API 接口清晰、知识域隔离、规则分发可靠。

### 2.4 知识治理视角

同一用户在治理知识资产时的关注点。

- 场景:定期检查知识库健康度--有多少盲区、多少过时条目待清理、检索命中率趋势如何;控制哪些 Agent 能访问哪些知识域。
- 设备:桌面浏览器。
- 关注点:治理自动化、可观测性、访问控制。

---

## 3. 痛点

用户在知识管理上连续遇到三个卡点:

- **知识散落,找不回来**(录入阶段):文档在本地文件夹、笔记在 Notion、对话记录在聊天窗口、决策在脑子里。想找某个之前做过的决定或看过的结论,翻遍三四个工具还不一定找得到。现有方案:手动整理到 Obsidian/Notion,但坚持不了两周就放弃,因为整理本身太耗时。
- **知识没有结构,看不出关联**(组织阶段):就算把资料集中到一个地方,也只是一堆文件的平铺列表。哪些概念之间有依赖关系、哪些方法适用于哪些场景、哪些结论之间有矛盾--全靠人脑记。现有方案:手动打标签、建文件夹层级,但标签体系维护成本高,跨主题关联几乎做不到。
- **不知道自己不知道什么**(进化阶段):知识库里有什么是清楚的,但哪些领域有盲区、哪些知识已经过时、哪些方向值得深入研究--没有任何工具能告诉你。现有方案:靠直觉和偶然发现,没有系统性的缺口检测和调研机制。

---

## 4. 原始需求

用户要的三件事:

1. **扔进去就能用**:把文档、笔记、链接、对话记录扔进系统,系统自动解析资料并萃取高价值知识、分门别类、建立关联。不需要手动整理,不需要定义模板,不需要学习复杂的组织方法。
1. **想找就能找到,还能发现意外关联**:搜索一个主题,不仅返回精确匹配,还能看到相关的方法、前置知识、易混点、来源材料。通过图谱浏览,发现自己没想到的知识关联。
1. **系统主动告诉我哪里有缺口,帮我补上**:系统自动检测知识盲区,提示哪些方向值得调研;用户一句话发起调研,Agent 执行并产出报告,报告采纳后自动入库。知识库越用越完整。

---

## 5. 用户体验流

设计原则:白色背景 + 黑色文字,浅色主题。AI 做所有结构化和关联工作,用户只负责喂数据、浏览结果和纠偏。内部技术概念(embedding、向量、cron、worker)不暴露给用户。

首次 10 分钟体验路径:

1. 用户进入材料库,上传一份材料。 → FR-W08 / FR-Z06
1. 系统完成识别、提取和归类,在材料卡片上展示处理进度和产出统计。 → FR-A02 / FR-K01 / FR-P04
1. 用户打开产出的 Wiki 页面,看到结构化摘要、来源材料和关联知识。 → FR-P06 / FR-W08
1. 用户在知识搜索中输入一个问题,验证系统能返回刚刚沉淀的结果。 → FR-W02 / FR-B02

全站导航结构(一级侧边栏,从上到下):

- 仪表盘
- 意图知识库
- 领域知识库(LLM Wiki)
- 知识搜索
- 知识图谱
- 材料库
- 调研队列
- 系统(含操作日志)
- 设置

### 5.1 材料导入旅程

用户把文件扔进系统,系统自动处理并产出结构化知识。

**步骤 1:进入材料库页面** → FR-W08

- 页面名称:材料库(`/materials`)
- 页面展示:已上传材料的卡片列表(按上传时间倒序),顶部有一个醒目的「上传材料」按钮
- 空状态:首次进入时展示引导文案「上传文档、图片、音频或视频,系统自动解析资料并萃取高价值知识」,附带上传按钮

**步骤 2:上传材料** → FR-W08

- 操作:点击「上传材料」按钮,或直接拖拽文件到页面
- 弹出上传区域展示:
  - 支持格式提示:PDF、图片、文本/Markdown、音频、视频
  - 单文件大小限制
  - 目标领域选择:默认「自动识别」,也可手动选择已有领域
- 用户选择文件后,文件立即开始上传

**步骤 3:上传中状态** → FR-W08 / FR-Z07

- 页面展示:材料卡片立即出现在列表第一位
- 卡片信息:文件名、文件大小、类型图标、上传进度条
- 状态文案:「上传中...」
- 上传失败时:卡片显示失败原因(文件太大/格式不支持/网络失败),提供「重试」按钮

**步骤 4:处理中状态** → FR-W08 / FR-A02 / FR-K01

- 上传成功后,卡片状态自动切换为处理流程
- 卡片展示人话进度步骤:
  - ✓ 已登记
  - ✓ 识别材料
  - → 提取内容(当前步骤高亮)
  - ○ 形成参考资料
  - ○ 归入领域
  - ○ 生成 Wiki
  - ○ 建立关系
- 长文档额外展示:「已处理 5/83 个片段」
- 不展示:embedding 模型名、向量库参数、cron 名称、worker 路径、prompt

**步骤 5:处理完成** → FR-A02 / FR-P04 / FR-P06

- 卡片状态变为「已完成」,展示产出统计:参考资料 N 条、Knowledge Entry N 条、Wiki 页面 N 个、关系 N 条
- 卡片底部出现三个操作按钮:
  - 「查看产出的内容」→ 跳转到材料详情页
  - 「进入领域知识库」→ 跳转到系统识别出的领域/主题节点
  - 「打开 Wiki 页面」→ 跳转到聚合后的 Wiki 页面

**步骤 6:查看材料详情** → FR-W08 / FR-P04 / FR-P06

- 页面名称:材料详情页(`/materials/:id`)
- 页面展示:
  - 文件摘要:文件名、类型、上传时间、处理状态、所属领域
  - 产出统计:切片数、知识点数、Wiki 页数、关系数
  - 知识列表:按类型分组(概念/方法/题目/易错点/批注),每条显示标题和摘要
  - 来源定位:每条知识旁显示页码/时间戳/图片区域
- 操作按钮:每条知识可「编辑」「删除」
- 页面跳转:点击知识条目 → 进入知识详情页;点击「进入领域」→ 跳转到领域知识库对应节点

### 5.2 领域知识浏览旅程

用户按学科/主题结构浏览系统沉淀的领域知识。

**步骤 1:进入领域知识库** → FR-P01 / FR-P08 / FR-W12

- 页面名称:领域知识库(`/wiki`)
- 页面展示:
  - 左侧:主题目录树(Subject Node 层级结构),顶层分为「通用知识」和「学科知识」两类
  - 右侧:当前选中节点下的 Wiki 页面列表
  - 每个目录节点显示:节点名称、下属条目数
- 默认状态:展开第一个有内容的主题节点
- 空状态:「还没有领域知识,上传材料后系统会自动归类到这里」

**步骤 2:浏览主题目录** → FR-P01 / FR-P09

- 操作:点击左侧目录树的节点,展开/收起子节点
- 目录层级:领域 → 子领域 → 具体主题(如:学科知识 → [某学科] → [某章节/主题])
- 每个节点可展开查看子节点,点击叶子节点时右侧展示该主题下的 Wiki 页面列表
- 节点旁显示该节点及其子节点下的总条目数

**步骤 3:查看 Wiki 页面列表** → FR-P06 / FR-P02

- 页面展示:选中主题节点后,右侧展示该主题下所有 Wiki 页面
- 每个 Wiki 页面卡片显示:
  - 标题(人话主题名,不是文件名)
  - 摘要(1-3 句话说明这页解决什么问题)
  - 标签:知识类型(概念/方法/题目等)、来源材料数、知识点数、关系数
  - 状态标记:AI 生成 / 用户已编辑 / 存在争议
- 操作:点击卡片 → 进入 Wiki 页面详情

**步骤 4:阅读 Wiki 页面** → FR-P06 / FR-P07 / FR-G05

- 页面名称:Wiki 详情页(`/wiki/:id`)
- 页面结构:
  - 面包屑导航:领域知识库 / [领域] / [主题] / [页面标题]
  - 标题 + 摘要 + 标签行(类型、来源数、知识点数、关系数、状态)
  - 主体正文(按知识类型组织:定义→公式→适用条件→例题→易错点→与相近概念的区别)
  - 说明:这里展示的是 Reference Material / Wiki 的知识结构示例,不是学习进度或学习交互场景;只有能指导决策或行动的提炼信息才可进入 Knowledge Entry。
  - 来源材料区:列出贡献该页面的原始文件,每条显示文件名/页码/段落,可点击「查看原文」
  - 关联知识区:前置知识、相关参考资料、解题方法、易混点(来自图谱关系)
  - 批注区:来自原材料的批注 + 用户在本页新增的批注
  - 底部 mini 图谱:展示当前页面前后 1 跳的关系节点
- 操作按钮:
  - 「编辑」→ 进入编辑模式,可修改标题、摘要、正文
  - 「在图谱中查看」→ 跳转到知识图谱页面,聚焦当前节点
  - 「查看来源材料」→ 跳转到对应材料详情页
  - 「添加批注」→ 在页面内选中文字后添加批注
- 关联知识可信度标记:每条关联标注来源类型(系统推断/同材料共现/用户确认),用户可确认或删除关系

**步骤 5:从 Wiki 页面跳转** → FR-P06 / FR-G03 / FR-W08

- 点击关联知识中的条目 → 跳转到对应 Wiki 页面或知识详情页
- 点击来源材料 → 跳转到材料详情页,定位到对应页码/片段
- 点击 mini 图谱节点 → 跳转到对应 Wiki 页面
- 点击面包屑 → 返回上级目录

### 5.3 搜索旅程

用户通过关键词或自然语言查找知识。

**步骤 1:进入搜索** → FR-W02 / FR-W12

- 入口位置:侧边栏「知识搜索」+ 全站顶部搜索框(任何页面可用)
- 页面名称:知识搜索(`/search`)
- 搜索框 placeholder:「搜索知识...」
- 搜索框下方:最近搜索历史(最近 10 条)

**步骤 2:输入搜索词** → FR-W02

- 操作:在搜索框输入关键词或自然语言描述
- 实时反馈:输入时展示搜索建议(基于已有知识标题)
- 提交搜索:按回车或点击搜索按钮

**步骤 3:查看搜索结果** → FR-W02 / FR-B02 / FR-E07

- 结果分类展示(Tab 切换):
  - 「全部」:混合展示所有类型结果,按相关度排序
  - 「领域知识」:仅展示 Wiki 页面和领域知识条目
  - 「意图知识」:仅展示意图类知识(纠偏/规则/偏好)
  - 「材料」:仅展示原始材料文件
- 每条结果展示:
  - 标题(关键词高亮)
  - 摘要片段(命中内容高亮)
  - 类型标签(概念/方法/意图/材料等)
  - 所属领域
  - 相关度指示(高/中,不展示具体分数)
- 结果支持按类型、领域、时间范围筛选
- 无结果时:展示「未找到相关知识」+ 建议操作(上传材料/发起调研)

**步骤 4:点击结果进入详情** → FR-W02 / FR-P06 / FR-W08

- 点击领域知识结果 → 跳转到 Wiki 详情页(`/wiki/:id`)
- 点击意图知识结果 → 跳转到意图知识详情页(`/intent/:id`)
- 点击材料结果 → 跳转到材料详情页(`/materials/:id`)

### 5.4 意图知识旅程

用户浏览和管理从对话中自动沉淀的意图知识(纠偏、规则、偏好、声明)。

**步骤 1:进入意图知识库** → FR-E04 / FR-W02 / FR-W12

- 页面名称:意图知识库(`/intent`)
- 页面展示:意图知识条目列表,按创建时间倒序
- 每条卡片展示:
  - 标题(一句话概括这条意图)
  - 信号类型标签:纠偏 / 强调 / 声明 / 规则 / 偏好
  - 来源:从哪次对话中提取
  - 触发频率:被注入过多少次
  - 创建时间
- 筛选:按信号类型筛选(全部/纠偏/强调/声明/规则/偏好)
- 搜索:列表内搜索框,按标题和内容过滤
- 空状态:「系统会从你和 Agent 的对话中自动提取意图知识,目前还没有」

**步骤 2:查看意图知识详情** → FR-E04 / FR-E06

- 页面名称:意图知识详情页(`/intent/:id`)
- 页面结构:
  - 标题
  - 信号类型 + 创建时间 + 来源对话引用
  - 正文内容(完整描述)
  - 正例列表:「用户想要的」(绿色标记)
  - 负例列表:「用户不想要的」(红色标记)
  - 相似句列表:用户可能用来表达同一意图的不同说法(标签样式)
  - 触发记录:最近被注入的场景列表
- 操作按钮:
  - 「编辑」→ 修改标题、正文、正例、负例
  - 「删除」→ 确认后删除
  - 「查看来源对话」→ 跳转到对话记录

**步骤 3:意图知识与领域知识的区别** → FR-E07 / FR-P02

- 意图知识回答:「Agent 应该怎么做」--用户的偏好、规则、纠偏记录
- 领域知识回答:「世界是什么样的」--概念、方法、事实、关系
- 两者在导航、页面结构、API 路由上完全独立,不混排
- 全局搜索中两者分 Tab 展示,卡片结构不同(意图卡片有正例/负例,领域卡片有来源材料/关系)

### 5.5 材料管理旅程

用户管理已上传的所有材料文件。

**步骤 1:进入材料库** → FR-W08

- 页面名称:材料库(`/materials`)
- 页面展示:所有已上传材料的卡片列表
- 每张卡片展示:
  - 文件名 + 类型图标(PDF/图片/音频/视频/文本)
  - 上传时间
  - 处理状态:已完成 / 处理中(含进度) / 失败
  - 所属领域(系统自动识别的)
  - 产出统计:参考资料 N 条、Knowledge Entry N 条(处理完成时显示)
- 排序:默认按上传时间倒序,可切换为按状态、按领域
- 筛选:按状态(全部/处理中/已完成/失败)、按领域

**步骤 2:查看材料详情** → FR-W08 / FR-P04

- 页面名称:材料详情页(`/materials/:id`)
- 页面结构:
  - 文件摘要:文件名、类型、大小、上传时间、处理状态、所属领域
  - 处理进度(如果仍在处理):步骤进度条 + 已处理片段数/总片段数
  - 产出统计:切片数、知识点数、Wiki 页数、关系数
  - 知识列表:该材料产出的所有知识条目,按类型分组
  - 每条知识显示:标题、摘要、来源定位(页码/时间戳/区域坐标)
- 操作按钮:
  - 「重新处理」→ 重新触发提取管线(用于处理失败或想更新提取结果时)
  - 「删除材料」→ 确认对话框,说明影响(删除材料及其产出的知识条目)
  - 「查看原文」→ 打开材料阅读器(PDF 预览/图片查看/音频播放)
  - 每条 Reference Material 或 Knowledge Entry 可「编辑」「删除」「查看在 Wiki 中的位置」

**步骤 3:材料操作** → FR-W08 / FR-A02 / FR-Z07

- 重新处理:点击后材料状态回到「处理中」,重新走提取管线,旧产出保留直到新产出完成
- 删除材料:弹出确认对话框,明确说明「将同时删除该材料产出的 N 条知识和 N 个 Wiki 页面」,确认后执行
- 批量操作:列表页支持多选,批量删除或批量重新处理

---

## 6. 核心概念

### 6.1 核心对象类型

- **Knowledge Entry**:知识条目,KIVO 管理的最小高价值知识单元。知识必须是经过抽象、聚合、萃取后形成的长效理解模型,能帮助 Agent 或用户在未来做出更好的判断。教材概念定义、公式、定理、例题、原文摘录不是 Knowledge Entry,只作为 Reference Material 或 Wiki 编译素材存在。粒度标准:一条 Knowledge Entry = 一个能独立说明适用场景、边界和行动价值的知识断言。
- **Knowledge Type**:知识类型枚举--fact、methodology、decision、experience、intent、meta。详见 §6.3。
- **Source Reference**:来源引用,记录知识的出处(对话 ID、文档路径、URL、规则文件路径等)。
- **Conflict Record**:冲突记录,记录两条知识之间的冲突、解决策略和解决结论。
- **Research Task**:调研任务,由用户通过对话发起,包含任务描述、状态和产出链接。
- **Rule Entry**:规则条目,可分发的系统规则。管"Agent 必须遵守什么约束",与 Knowledge Entry(管"Agent 知道什么")职责分离。
- **Subscription**:订阅关系,记录 Agent 与规则集之间的绑定。
- **Distribution Record**:分发记录,记录规则推送的目标、时间和确认状态。
- **Research Queue**:调研队列,展示用户通过对话发起的调研任务及其状态和产出链接。
- **Analysis Artifact**:分析中间产物,知识提取过程中产生的结构化分析结果,包含候选实体、主题、关联建议、冲突建议和调研建议。
- **Reference Material**:参考资料,由教材、讲义、题目、公式、定理、例题、原文摘录、批注等整理出的可溯源材料。参考资料用于 Wiki 编译、检索和溯源,不进入 entries 表,不参与 Knowledge Entry 的治理和注入。用户主动标记其中某段内容具有决策/行动价值时,系统可从中萃取 Knowledge Entry,但必须先通过统一质量门禁。
- **Knowledge Graph**:知识图谱,基于 Knowledge Entry 之间的关联关系构建的网络结构,支撑洞察发现和可视化探索。
- **Domain Purpose**:知识域目标声明,定义每个知识域的目标、关键问题、非目标和研究边界,指导知识采集和治理方向。

### 6.2 知识条目状态

知识条目的生命周期状态只有一种:**active**(生效,可被检索和引用)。

不存在 pending、archived、superseded、deprecated 等中间状态。提取质量不够的知识直接丢弃,不入库;过时的知识直接删除,不保留历史。知识库只保留当前有效的知识。

多状态生命周期会增加无效复杂度:pending 条目需要逐条审核,archived 条目长期占用空间但很少被查看。KIVO 简化为单状态,提取时直接决定入库或丢弃。

### 6.3 知识类型判别规则

<lark-table rows="7" cols="5" header-row="true" column-widths="146,146,146,146,146">

  <lark-tr>
    <lark-td>
      类型
    </lark-td>
    <lark-td>
      定义
    </lark-td>
    <lark-td>
      判别关键
    </lark-td>
    <lark-td>
      正例
    </lark-td>
    <lark-td>
      反例
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      fact
    </lark-td>
    <lark-td>
      对客观世界的陈述,可独立验证真假
    </lark-td>
    <lark-td>
      描述"是什么",不含主观判断
    </lark-td>
    <lark-td>
      "Node.js 22 默认启用 ESM"
    </lark-td>
    <lark-td>
      "应该用 Node.js 22"(decision)
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      methodology
    </lark-td>
    <lark-td>
      可复用的做事方法,已抽象为通用步骤
    </lark-td>
    <lark-td>
      不绑定某次具体实践
    </lark-td>
    <lark-td>
      "先写失败测试,再写最小实现"
    </lark-td>
    <lark-td>
      "上次我先写了测试效果不错"(experience)
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      decision
    </lark-td>
    <lark-td>
      在多个方案中做出的明确选择
    </lark-td>
    <lark-td>
      含"选择 X 而不选 Y"结构
    </lark-td>
    <lark-td>
      "本项目采用 PostgreSQL 而非 MongoDB"
    </lark-td>
    <lark-td>
      "PostgreSQL 支持事务"(fact)
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      experience
    </lark-td>
    <lark-td>
      某次具体实践的经历和教训
    </lark-td>
    <lark-td>
      绑定特定上下文,未抽象为通用方法
    </lark-td>
    <lark-td>
      "上次迁移 ESM 时因没检查依赖兼容性回滚了两次"
    </lark-td>
    <lark-td>
      "迁移 ESM 前应先检查依赖兼容性"(methodology)
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      intent
    </lark-td>
    <lark-td>
      用户的偏好、目标、约束或期望
    </lark-td>
    <lark-td>
      表达主观意愿,可能随时间变化
    </lark-td>
    <lark-td>
      "用户要求代码注释用中文"
    </lark-td>
    <lark-td>
      "代码注释必须用中文"(若为强制规范则进 Rule Entry)
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      meta
    </lark-td>
    <lark-td>
      关于知识管理本身的反思
    </lark-td>
    <lark-td>
      主题是知识过程本身
    </lark-td>
    <lark-td>
      "连续三次纠偏同一类问题时应发起专项调研"
    </lark-td>
    <lark-td>
      "每周回顾一次项目进度"(methodology)
    </lark-td>
  </lark-tr>
</lark-table>

---

## 7. 功能需求(FR)

### 功能需求分层

FR 按用户价值链分为三层,帮助读者快速定位每个域的定位和优先级:

**核心用户功能(Core)**:用户直接感知和使用的能力,直接解决三大痛点(知识散落、缺乏结构、不知道盲区)。

- 域 A:知识提取 - 从对话、文档、多模态材料中自动提取结构化知识
- 域 B:知识存储与检索 - 结构化存储、语义检索、关联维护
- 域 C:知识迭代 - 知识更新、冲突解决、一致性维护
- 域 D:调研管理 - 用户发起调研、Agent 执行、报告采纳入库
- 域 E:意图理解增强 - 意图知识提取、注入、治理
- 域 G:知识图谱与洞察 - 图谱构建、结构洞察、可视化探索
- 域 L:分析中间产物 - 提取过程的结构化分析结果,支撑审计和缺口发现
- 域 M:知识域目标声明 - 定义知识域的目标和边界,指导采集与治理
- 域 N:知识治理 - 准入门禁、去重、质量审计、碎片聚合、定期存量治理
- 域 W:知识工作台 - 面向用户的 Web UI,知识浏览、搜索、导入、管理
- 域 P:领域知识库(LLM Wiki) - 领域知识的组织、Wiki 聚合、目录结构

**平台支撑能力(Platform)**:保障系统稳定运行和 Agent 集成的基础设施,用户不直接操作但间接受益。

- 域 F:规则订阅与分发 - 系统规则的注册、订阅和推送
- 域 H:系统词典 - 术语统一、消歧、跨模块一致性
- 域 I:OpenClaw 集成层 - 与宿主环境的能力声明、插件对接
- 域 K:知识管线编排 - 管线阶段编排、分类路由
- 域 X:访问控制与可观测性 - 权限隔离、审计日志、运行监控

**商用化扩展(Commercial)**:面向产品化交付的能力,确保陌生用户开箱即用。

- 域 Z:开箱即用与商用就绪 - 安装引导、首次体验、配置管理、文档交付

---

### 域 A:知识提取(Knowledge Extraction)

负责从各类来源中提取结构化知识。

#### FR-A01:对话知识提取

从 Agent 与用户的对话中自动提取结构化知识。提取管线精简为五步:原子分解+去上下文化 → 行为变化测试+三重测试(准入门禁) → 向量去重 → 素材暂存 → 抽象聚合入库。

- AC-A01-1:系统能从对话中识别并提取六类知识:fact、methodology、decision、experience、intent、meta。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A01-2:提取结果包含 LLM 归纳的短标题(≤20字,语义完整的短句或短语,是对知识点的提炼概括,禁止硬截断)、LLM 归纳的结构化内容描述(不限字数,要求清晰讲透知识点的含义、适用场景和边界,确保被检索命中后能为 Agent 提供足够上下文做出正确判断)、知识类型、置信度、来源引用、提取时间和 2-3 个相似表述/泛化例句。标题和内容必须经过 LLM 抽象归纳,不能直接照搬原文。置信度仅用于决定是否入库,入库后不保留为条目属性。禁止 description 复制 title;description 必须比 title 提供更多信息。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-A01-3:置信度低于可配置阈值的提取结果直接丢弃,不入库。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A01-4:提取完成后,置信度达标的条目自动写入知识库;低置信度条目直接丢弃。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A01-5:提取过程产出 Analysis Artifact(域 L),记录分析中间结果,支持审计和回放。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A01-6:原子分解必须同时完成去上下文化(decontextualization)--解析代词指代、补全省略的实体名称、将隐含的上下文信息显式化。产出的每条原子知识必须脱离原始对话上下文后仍可独立理解。原子分解与去上下文化在同一个 LLM 调用中一步完成,不拆为两个阶段。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A01-7:行为变化测试是知识入库的唯一准入门禁。判断标准由三重测试组成,全部通过才允许入库,任一不通过直接丢弃:；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
  - 时效性测试:「一个月后这条信息还有效吗?」临时状态、一次性决策、排查中间步骤等时效性信息不通过。
  - 跨场景测试:「换一个完全不同的项目/场景,它还适用吗?」绑定特定项目、特定时间窗口的信息不通过。
  - 抽象性测试:「去掉具体的人名、项目名、时间,它是否仍有指导价值?」未经抽象的具体事件记录不通过。
  - 行为影响测试:「如果这条知识不存在,agent 会做出不同的(错误的)决策吗?」回答为「否」不通过。三重测试由 LLM 语义判断执行,禁止使用关键词匹配、正则表达式、FTS5、规则引擎承担语义理解职责。明确的负例(以下内容禁止入库):一次性调度决策(「先做A再做B」)、临时优先级排序(「这周先搞官网」)、排查中间步骤(「加日志定位根因」)、任务派发指令(「codex 去做这个」)、未经抽象的具体事件记录(「今天 cc 超时了」)、配置描述(「当前用 penguin provider」)、待办事项(「需要加个监控脚本」)。明确的正例(这些是知识):跨场景可复用的原则(「关键词匹配不能承担语义理解职责」)、长效有效的方法论(「修复问题先堵根因再修症状」)、抽象的理解模型(「LLM 始终可用,不存在无 LLM 降级场景」)。
- AC-A01-8:应该提取的知识类型--用户私有决策(LLM 不可能自己知道)、用户私有术语/黑话、反复出现的 badcase(LLM 知道但做不到)、用户特有的偏好和约束、容易被 context 稀释遗忘的关键约束。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A01-9:禁止提取的内容--通用常识(LLM 本来就会)、概念解释(LLM training data 里有)、通用方法论教程(LLM 本身就知道怎么做)、非私有的事实性描述。此类内容即使出现在对话中也必须跳过,不生成知识条目。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A01-10:提取粒度为原子级--单次提取产出的是原子级知识片段:一条决策、一条纠偏、一条事实、一条偏好。每条原子知识通常一两句话能说清。禁止从对话中一次性摘出大段文字当知识条目。禁止产出百科式解释、教程式方法论、步骤式操作指南。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A01-11:复杂知识只能通过合并加工产出,不能在提取阶段直接生成。流程:多次提取原子片段 → 发现多条指向同一主题 → 合并加工成一条更完整的知识(由域 N 治理流程或域 C 迭代流程负责)。合并后的知识条目可以较长,但必须是加工产物,不是原始摘录。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A01-12:入库前向量去重--对候选知识做 embedding,与已有知识库计算 cosine 相似度,>= 0.92 视为重复,重复条目跳过入库。去重阈值 0.92 可通过 kivo.config.json 的 `extraction.dedup.threshold` 配置。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。

#### FR-A02:多模态文档编译管线

从文档、图片、音频、视频等多模态材料中提取结构化知识。按内容特征路由解析器,不按来源分类。

**输入内容类型:**

- 文档:PDF、PPT、Word、Markdown、纯文本、EPUB、笔记 App 导出(GoodNotes/Notability/OneNote/MarginNote 等)
- 图片:拍照(教材/板书/手写笔记/错题本)、截图
- 音频:课堂录音、讲座录音(转写后进入知识提取管线)
- 视频:录屏、录课视频、下载的网课视频(音轨转写+关键帧抽取,双通道提取)
- 网页/链接:任意 URL,抓取后按内容特征路由

**输入入口:**

- 飞书对话直接发送(碎片化、随时随地)
- Web 端上传(系统性整理,带进度条)
- 对话中链接/URL(含非结构化云笔记页面)

三个入口同一条管线。URL 只是获取方式,获取后走同一条解析路由。

**中间表示(Document AST)分层:**

- 原始内容层:教材/讲义的原文
- 批注/强调层:手写旁注、高亮、圈画、箭头、颜色标记、录音中的口头强调
- 关联关系:批注→内容定位、时间戳→内容定位

**批注作为知识提取的权重信号:**

- 被高亮/圈画/批注的内容 → 提取优先级更高
- 手写旁注含"搞混""不懂""重要"等 → 直接标记为薄弱点/易错点
- 录音中口头强调 → 同等权重信号
- 没有批注的内容 → LLM 根据内容语义判断参考价值(核心定义/定理/公式可作为 Wiki 编译素材,但不得直接作为 Knowledge Entry 入库)
- 用户批注与 LLM 判断冲突时 → 用户批注优先

**验收标准:**

- AC-A02-1:支持上述所有输入内容类型,按内容特征自动路由到对应解析器。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-A02-2:提取结果保留原文引用位置(页码/时间戳/截图坐标),支持溯源。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A02-3:长文档支持分段提取,每段独立标注知识类型和关联关系。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A02-4:提取完成后,多模态材料先生成 Reference Material、Analysis Artifact 和 Wiki 编译素材,不直接写入 entries 表。只有用户主动标记或下游场景明确需要沉淀为高价值知识时,才从材料中萃取 Knowledge Entry,并经过统一质量门禁后入库。低置信度素材进入待确认队列。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A02-5:提取过程产出 Analysis Artifact(域 L),记录候选实体、主题、关联建议等中间结果。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A02-6:手写内容通过 OCR 识别(PaddleOCR 本地优先,Mathpix 作为付费增强),公式还原为 LaTeX。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A02-7:视频输入提取音轨转写+关键帧 OCR,双通道合并为统一知识流。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A02-8:单文件上限 400MB,大文件流式分片处理。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-A02-9:文档导入状态管理:上传中/解析中/完成/失败/待确认。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-A02-10:材料处理保真度--禁止对单份材料的提取分片数量设置硬上限截断。长文档按分片分批排队处理,所有分片处理完成后才将该材料标记为 done。处理中的材料状态为 processing,前端可查看已完成分片数/总分片数进度。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-A02-10:原始文档与提取出的 Reference Material、Wiki 页面和 Knowledge Entry 保持溯源关系(哪个文件的哪一页/哪个时间戳)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-A02-11:图片内容类型识别--管线对每张图片/扫描页先做内容分类,再路由到对应提取策略:；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
  - 数学公式/推导(含板书公式):提取为 Reference Material,每个独立定理、公式、推导链路作为可溯源素材单元保存,LaTeX 还原后供 Wiki 编译和检索使用。
  - 流程图/架构图/思维导图:提取为 methodology 类型,还原节点与边的逻辑关系,保留流程步骤顺序。
  - 数据表格/统计图(柱状图、折线图、饼图等):提取为 fact 类型,结构化为数据点+结论摘要。
  - 手写笔记(含个人缩写、涂改):先 OCR 识别文字(复用 AC6 的 PaddleOCR/Mathpix),再按识别后的文本内容分类提取。
  - 代码截图/伪代码:提取为 fact 类型,还原为可复制的代码文本,标注语言和用途。
  - 板书/课堂截图(混合内容):拆分为多条 Reference Material 素材单元。概念定义、公式和例题作为参考资料保存;只有用户批注、易错原因、解题策略等能指导行动的内容,才可萃取为 Knowledge Entry 并进入门禁。
  - 纯文字页面(印刷体教材扫描):走文本提取管线,不走图片管线。
- AC-A02-12:单图多素材单元拆分--一张图片/一页扫描件可能包含多个独立素材单元(如一页讲义含 3 个定理、2 个例题),管线必须将每个独立素材单元拆分为独立的 Reference Material 记录,禁止将整页内容合并为一条记录。拆分粒度标准:能独立被检索命中并在 Wiki 编译时提供完整上下文的最小单元。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-A02-13:图片内素材关联--同一图片/页面中拆分出的多条 Reference Material 之间,通过材料关系表达关联(如:定理→推论、定义→例题、前提→结论)。这些关系用于 Wiki 编译;只有对应内容被萃取并通过门禁成为 Knowledge Entry 后,才进入知识图谱。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-A02-14:图片来源精确溯源--每条从图片/扫描件提取的知识条目必须记录:；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
  - 来源文件路径/ID
  - 页码(PDF 页码或图片序号)
  - 图片内区域坐标(bounding box,像素级定位)
  - 提取时使用的解析器类型(OCR/视觉 LLM/混合)
- AC-A02-15:前端图片溯源交互--用户在参考资料、Wiki 页面或知识条目详情页点击"查看来源"时,系统展示原始图片并高亮标注对应区域(基于 AC14 存储的坐标),支持缩放和上下文浏览(前后页)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-A02-16:图片类型识别置信度--当图片内容分类置信度低于阈值时(如手写潦草难以判断是公式还是文字),标记为"待人工确认",进入待确认队列(复用 AC4 的低置信度机制),用户可在前端修正分类后重新触发提取。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

**真实验收测试集(2026-05-23 用户提供):**

以下 21 个文件来自中国普通本科大二学生的真实学习资料,覆盖管线必须处理的全部场景:

<lark-table rows="22" cols="4" header-row="true" column-widths="183,183,183,183">

  <lark-tr>
    <lark-td>
      #
    </lark-td>
    <lark-td>
      文件
    </lark-td>
    <lark-td>
      类型
    </lark-td>
    <lark-td>
      管线挑战
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      1
    </lark-td>
    <lark-td>
      高斯课堂·二重积分讲义(4页)
    </lark-td>
    <lark-td>
      辅导机构 PDF
    </lark-td>
    <lark-td>
      短文档、排版紧凑、含公式
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      2
    </lark-td>
    <lark-td>
      高斯课堂·二重积分习题答案
    </lark-td>
    <lark-td>
      辅导机构 PDF
    </lark-td>
    <lark-td>
      题目+解答混排
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      3
    </lark-td>
    <lark-td>
      高斯课堂·概率论练习题答案
    </lark-td>
    <lark-td>
      辅导机构 PDF
    </lark-td>
    <lark-td>
      多课时练习详解
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      4
    </lark-td>
    <lark-td>
      高斯课堂·概率论讲义笔记
    </lark-td>
    <lark-td>
      辅导机构 PDF
    </lark-td>
    <lark-td>
      完整课时讲义
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      5
    </lark-td>
    <lark-td>
      概率论笔记
    </lark-td>
    <lark-td>
      学生笔记 PDF
    </lark-td>
    <lark-td>
      可能含手写
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      6
    </lark-td>
    <lark-td>
      浙大·概率论与数理统计(第四版)
    </lark-td>
    <lark-td>
      正式教材 PDF
    </lark-td>
    <lark-td>
      大部头、章节结构复杂
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      7
    </lark-td>
    <lark-td>
      浙大第四版·习题全解指南
    </lark-td>
    <lark-td>
      配套习题解答 PDF
    </lark-td>
    <lark-td>
      题号对应、解题步骤
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      8
    </lark-td>
    <lark-td>
      二轮高数强化手写笔记
    </lark-td>
    <lark-td>
      手写笔记 PDF
    </lark-td>
    <lark-td>
      OCR 挑战、个人缩写
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      9
    </lark-td>
    <lark-td>
      李永乐线代白底版
    </lark-td>
    <lark-td>
      考研名师 PDF
    </lark-td>
    <lark-td>
      跨学科(线代)
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      10
    </lark-td>
    <lark-td>
      茆诗松·概率论简明教程
    </lark-td>
    <lark-td>
      正式教材 PDF
    </lark-td>
    <lark-td>
      另一体系教材
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      11
    </lark-td>
    <lark-td>
      李永乐线代黑底版
    </lark-td>
    <lark-td>
      考研名师 PDF
    </lark-td>
    <lark-td>
      同内容不同底色,去重挑战
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      12
    </lark-td>
    <lark-td>
      茆诗松·概率论教程(完整版)
    </lark-td>
    <lark-td>
      正式教材 PDF
    </lark-td>
    <lark-td>
      大部头
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      13
    </lark-td>
    <lark-td>
      茆诗松第2版·笔记+习题+考研真题详解
    </lark-td>
    <lark-td>
      辅助材料 PDF
    </lark-td>
    <lark-td>
      多层内容混排
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      14
    </lark-td>
    <lark-td>
      茆诗松第2版·配套题库(四合一)
    </lark-td>
    <lark-td>
      题库 PDF
    </lark-td>
    <lark-td>
      考研真题+课后+章节+模拟
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      15
    </lark-td>
    <lark-td>
      茆诗松第3版·配套题库
    </lark-td>
    <lark-td>
      题库 PDF
    </lark-td>
    <lark-td>
      版本差异
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      16
    </lark-td>
    <lark-td>
      茆诗松·习题与解答
    </lark-td>
    <lark-td>
      习题解答 PDF
    </lark-td>
    <lark-td>
      纯解答
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      17
    </lark-td>
    <lark-td>
      茆诗松第3版·笔记+习题详解
    </lark-td>
    <lark-td>
      辅助材料 PDF
    </lark-td>
    <lark-td>
      版本差异
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      18
    </lark-td>
    <lark-td>
      浙大第5版·配套题库
    </lark-td>
    <lark-td>
      题库 PDF
    </lark-td>
    <lark-td>
      新版本
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      19
    </lark-td>
    <lark-td>
      浙大第5版·课后答案
    </lark-td>
    <lark-td>
      课后答案 PDF
    </lark-td>
    <lark-td>
      纯答案
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      20
    </lark-td>
    <lark-td>
      课程视频(14分钟)
    </lark-td>
    <lark-td>
      视频 MP4
    </lark-td>
    <lark-td>
      音轨转写+关键帧 OCR
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      21
    </lark-td>
    <lark-td>
      一轮高数基础手写笔记
    </lark-td>
    <lark-td>
      手写笔记 PDF
    </lark-td>
    <lark-td>
      OCR 挑战
    </lark-td>
  </lark-tr>
</lark-table>

管线必须能处理的场景覆盖:

- 同一教材多版本(浙大4版/5版,茆诗松2版/3版)→ 版本感知与关联
- 同内容不同呈现(李永乐白底/黑底)→ 内容去重
- 跨学科混投(概率论+线代+高数)→ 自动领域分类
- 体量差异(4页讲义 vs 数百页教材)→ 分片策略自适应
- 手写笔记 → OCR + 个人缩写理解
- 视频 → 音轨转写 + 关键帧提取
- 教材+配套习题解答 → 自动关联(题号对应)

#### FR-A03:规则提取

从系统配置、AGENTS.md、SOUL.md 等治理文件中提取可分发的规则条目。

- AC-A03-1:规则条目包含规则内容、适用范围、优先级和生效条件。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-A03-2:规则变更时自动检测差异,生成变更事件。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A03-3:规则之间的冲突和覆盖关系被显式记录。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-A04:个人知识录入

支持用户通过多种入口主动录入知识。

- AC-A04-1:支持手动录入--用户在 Workbench 中直接创建知识条目,填写标题、内容、类型和标签。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-A04-2:支持文件导入--用户上传文档文件,系统自动解析资料并萃取高价值知识(复用 FR-A02 文档提取管线)。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A04-3:支持 URL 抓取--用户输入网页 URL,系统抓取正文并提取知识。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A04-4:支持对话沉淀--用户在 Workbench 中标记对话片段为"值得保留",系统提取并入库。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A04-5:支持批量文件夹导入--用户指定本地目录,系统递归扫描并逐文件提取知识,展示导入进度。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-A04-6:所有录入入口产出的知识条目走同一条管线(提取 → 冲突检测 → 入库),不绕过一致性保障。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A04-7:Web 端材料库页面(`/materials`)提供统一上传入口,支持拖拽和点击选择文件;上传后材料卡片立即出现在列表顶部,展示处理进度(详见 §5.1 材料导入旅程)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

#### FR-A05:会话历史知识萃取(Session Transcript Knowledge Extraction)

从用户与 Agent 的历史对话归档中批量提取有价值的知识。采用两阶段架构:

**Stage 1:素材采集** - 从对话中识别有潜在知识价值的片段,产出「素材」暂存在临时区域,不直接入库。素材是原始的、未经抽象的候选片段。

**Stage 2:抽象聚合** - 对同主题的多条素材进行综合,产出经过抽象的知识条目。只有 Stage 2 的产出才能进入入库流程(经三重测试 FR-A07)。聚合维度:同一主题在不同时间/场景下的多次表达 → 收敛为一条跨场景原则。已有知识库中存在相关条目时,优先更新/合并而非新增。

FR-A01 处理实时对话流中的知识提取;FR-A05 处理已归档的 session 文件,覆盖产品思路、决策推理、纠偏意图等高价值但未被沉淀的对话内容。两者互补:FR-A01 管增量,FR-A05 管存量。

与 FR-E04(意图知识提取)的关系:FR-E04 专注从对话中提取 intent 类型知识(正例/负例/信号);FR-A05 提取所有知识类型(fact/methodology/decision/experience/intent/meta),覆盖面更广,提取粒度更粗。两者可共存,FR-E04 的意图提取结果可作为 FR-A05 的去重参考。

依赖 FR-B05(多维知识标签体系):提取的知识条目使用三维标签(nature/function/domain)而非单一 type 字段。

前置条件:用户已配置可用的 embedding provider(如 Ollama + bge-m3、OpenAI embedding 等)。未配置 embedding provider 时,命令报错并输出配置引导,不静默跳过。

- AC-A05-1:CLI 入口 `kivo extract-sessions` 可用,`--help` 输出命令说明、参数列表和使用示例。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-A05-2:对真实 OpenClaw session JSONL 文件运行,能从用户消息中提取结构化素材并暂存到临时区域(staging),经 Stage 2 聚合后才写入知识库。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A05-3:噪音过滤有效--纯操作性对话(重启服务、查状态、部署、文件操作等)不被提取为知识条目。噪音过滤采用关键词规则,不依赖 LLM。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-A05-4:未配置 embedding provider 时,命令报错退出(非零退出码),输出明确的配置引导信息(推荐 Ollama + bge-m3 方案及替代方案),引导用户完成配置后重试。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-A05-5:`--dry-run` 模式只统计可提取的 Reference Material、候选 Knowledge Entry 数量和分类分布,不写入数据库。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A05-6:增量处理--系统记录已处理的 session 文件标识,重复运行不重复提取。支持 `--since DATE` 参数限定处理范围。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A05-7:提取的知识条目携带三维标签(nature/function/domain),标签由 LLM 在提取时同步生成。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A05-8:提取流程为:JSONL 解析 → 用户消息提取 → 关键词噪音过滤 → BGE embedding 生成 → 语义聚类去重 → LLM 结构化知识提取(含三维标签) → 标准知识管线入库。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-A05-9:`--limit N` 参数限制单次处理的 session 文件数量,用于控制 LLM 调用成本。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A05-10:定时萃取的用户活跃判定必须基于外部渠道的真实用户消息(如 Gateway 日志中的飞书 DM 入站记录),禁止使用 session 文件 mtime 作为判定依据。session 文件 mtime 会被系统内部事件(子 Agent completion event、heartbeat、内部消息路由)更新,与用户是否在对话无因果关系。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A05-11:「有对话但未提取」通知(written=0 告警)仅在同时满足以下两个条件时发送:1指定时间窗口内存在来自外部渠道的真实用户消息;2本轮萃取 written=0。系统内部事件不计入用户活跃判定,不触发此通知。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A05-12:禁止使用消息字数、字符数、段落长度、消息条数作为是否提取的判断条件。禁止在代码中设置 MIN_SEGMENT_LEN、MIN_MESSAGE_LENGTH、MIN_QUEUE_SIZE 或任何基于数量阈值的预过滤逻辑。知识价值与消息长度无因果关系--用户最高价值的纠偏可能只有几个字。正确做法:将用户消息全量聚合后交给 LLM 语义判断是否有提取价值,由 LLM 决定「无高价值内容」而非代码预过滤。唯一允许的预过滤:纯系统生成内容(heartbeat、system prompt injection、内部路由消息等非用户产出内容)。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-A05-13:提取流程中的去重判断必须使用 BGE 向量余弦相似度,禁止使用 FTS5 token 匹配、关键词重叠检测或正则表达式做去重。extract-queue-worker 的去重逻辑适用此约束。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-A05-14:萃取产出必须遵守 FR-A01 AC6-AC11 定义的准入标准、原子粒度和三重校验。LLM 提取 prompt 中必须包含准入判定规则和原子粒度约束,从源头控制产出质量,而非仅依赖下游门禁过滤。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A05-15:Stage 1 产出的素材不得直接写入 entries 表。素材暂存在独立的 staging 区域(staging_materials 表或等效存储),等待 Stage 2 聚合处理。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-A05-16:Stage 2 抽象聚合必须由 LLM 执行语义综合,禁止使用关键词匹配、正则表达式、FTS5、规则引擎承担语义理解职责。聚合逻辑:对同主题的多条素材进行语义综合,提炼出跨场景、跨时间的抽象原则。聚合的目标是产出更少、更精、更抽象的知识条目,而非更多。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-A05-17:聚合后的知识条目必须通过 FR-A07 定义的三重测试(时效性、跨场景、抽象性),全部通过后才能进入入库流程。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-A05-18:单次提取的素材数量不设硬编码上限,由 LLM 判断「是否有提取价值」。禁止在代码中设置 MAX_MATERIALS_PER_SESSION 或任何基于数量的截断逻辑。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A05-19:聚合时已有知识库中存在相关条目时,优先更新/合并已有条目而非新增。合并策略:保留更抽象、更完整的表述,来源引用取并集。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-A05-20:Stage 2 聚合的触发时机:每次 Stage 1 完成后自动触发一次聚合,对 staging 区域中所有未聚合的素材执行语义聚类和抽象提炼。聚合完成后清除已消费的素材。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。

#### FR-A06:实时知识提取(Real-time Knowledge Capture)

从 Agent 与用户的实时对话中自动捕获高价值知识,每轮对话结束后自动分析并提取。FR-A01 定义对话知识提取的通用能力;FR-A06 定义自动化触发机制和高价值知识的识别策略,确保纠偏、决策、新规则、事实更新、经验教训等关键知识不因对话结束而丢失。

- AC-A06-1:每轮对话结束后自动触发知识提取,分析当前轮次的用户消息和 Agent 回复。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A06-2:LLM 识别并提取高价值知识类型:纠偏(correction)、决策(decision)、新规则(rule)、事实更新(fact_update)、经验教训(lesson_learned)。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A06-3:提取的知识条目经统一质量门禁(FR-N05)过滤后再入库,不绕过门禁直接写入。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A06-4:知识识别必须使用 LLM 语义理解,禁止关键词匹配或规则引擎。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-A06-5:提取结果包含来源追溯信息:session_id、message_id、对话时间戳,支持从知识条目反查原始对话上下文。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A06-6:hook 执行异步化,不阻塞对话主流程,提取失败不影响用户正常对话。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-A06-7:同一轮对话中提取的多条知识条目各自独立经过门禁,不因其中一条被拒绝而影响其他条目。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A06-8:禁止使用消息字数、字符数、消息条数作为是否触发提取或是否入队的判断条件。禁止在代码中设置 MIN_MESSAGE_LENGTH、MIN_QUEUE_SIZE 或任何基于数量阈值的预过滤逻辑。每条用户消息到达时立即入队并触发提取流程,由 LLM 语义判断是否包含高价值知识,而非由代码根据长度或条数预先过滤。唯一允许的预过滤:纯系统生成内容(heartbeat、system prompt injection、内部路由消息等非用户产出内容)。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-A06-9:实时提取必须遵守 FR-A01 AC6-AC11 定义的准入标准、原子粒度和三重校验。提取 prompt 中必须包含准入判定规则和原子粒度约束,从源头控制产出质量,而非仅依赖下游门禁过滤。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。

#### FR-A07:知识定义与准入边界(Knowledge Definition & Admission Boundary)

定义「知识」的本质和边界,为所有写入 entries 表的路径提供统一的准入判定标准。

知识的定义:经过抽象、聚合、萃取后形成的长效理解模型。跨时间、跨场景可复用。缺了它 Agent 或用户会在未来判断、行动或决策中犯错。知识库存在的唯一目的是让 Agent 和用户在未来做出更好的判断。每条知识必须能回答:「它让 Agent 或用户在什么场景下避免了什么错误、做出什么更好的行动」。回答不了的就不该进入 entries 表。

教材概念定义、公式、定理、例题、题目解析、原文摘录、百科式解释、通用事实不属于 Knowledge Entry。它们可以作为 Reference Material、Wiki 页面素材、来源溯源和检索对象存在,但不得直接写入 entries 表。只有当材料被用户主动标记,或被系统萃取成能指导决策/行动的提炼信息,并通过本节准入测试和 FR-N05 质量门禁后,才可生成 Knowledge Entry。

知识的判定标准(三重测试,全部通过才入库):

1. 时效性测试:一个月后这条信息还有效吗?
1. 跨场景测试:换一个完全不同的项目/场景,它还适用吗?
1. 抽象性测试:去掉时间和场景上下文后,它是否仍有指导价值?

明确的负例(以下不是知识,禁止入库):

- 一次性调度决策(「先做A再做B」)
- 临时优先级排序(「这周先搞官网」)
- 排查中间步骤(「加日志定位根因」)
- 任务派发指令(「codex 去做这个」)
- 未经抽象的具体事件记录(「今天 cc 超时了」)
- 配置描述(「当前用 penguin provider」)
- 待办事项(「需要加个监控脚本」)
- 教科书概念定义(「条件概率的定义是……」)
- 公式/定理/例题的机械搬运(「二重积分由积分区域、被积函数和面积元素组成」)
- 原文摘录或改写(「教材第 3 页解释了某概念」)

明确的正例(这些是知识):

- 跨场景可复用的原则(「关键词匹配不能承担语义理解职责」)
- 长效有效的方法论(「修复问题先堵根因再修症状」)
- 抽象的理解模型(「LLM 始终可用,不存在无 LLM 降级场景」)
- AC-A07-1:所有写入 entries 表的路径必须执行三重测试(时效性、跨场景、抽象性)和行为影响测试,任一不通过则拒绝入库。三重测试与 FR-A01 AC7 的行为变化测试共同构成完整的准入门禁体系。Reference Material、Analysis Artifact 和 Wiki 编译素材不写入 entries 表,不冒充 Knowledge Entry。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-A07-2:三重测试由 LLM 语义判断执行,禁止使用关键词匹配、正则表达式、FTS5、规则引擎承担语义理解职责。所有语义判断必须走 LLM 或向量检索。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-A07-3:负例清单可扩展,通过配置文件或知识库自身维护。新发现的负例模式可动态追加到负例库中。；验收时修改配置后重新执行同一操作；新配置在下一次调用生效，返回值或记录字段体现配置变化。
- AC-A07-4:准入判定的 LLM prompt 必须包含完整正例和负例,确保 LLM 有充分的边界参考做出判断。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-A07-5:每条知识入库时必须能回答「它让 Agent 或用户在什么场景下避免了什么错误、做出什么更好的行动」。回答不了的条目拒绝入库。这是知识库存在的目标结果导向约束。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-A07-6:任何领域材料管线、Wiki 管线、文档导入管线不得以“领域知识”“学科知识”“参考资料”为名绕过本边界。若产物只是概念、公式、定理、例题、题目解析或原文摘要,只能保存为 Reference Material 或 Wiki 编译素材。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

### 域 B:知识存储与检索(Knowledge Store & Retrieval)

负责知识的结构化存储、版本管理和高效检索。

#### FR-B01:结构化知识存储

按知识类型分类存储,支持版本追踪。

- AC-B01-1:每条知识条目至少包含:ID、类型、内容、来源、创建时间、更新时间、版本号、状态、所属知识域。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-B01-2:知识条目只有 active 状态。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-B01-3:同一知识条目的历史版本可追溯。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-B01-4:KnowledgeEntry 支持 domain-specific metadata 扩展,不同域可在 metadata 字段中挂载结构化子 schema。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。

#### FR-B02:语义检索

支持基于语义相似度的知识检索。

- AC-B02-1:Agent 查询时,系统返回语义最相关的知识条目,按相关度排序。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-B02-2:检索结果包含知识条目内容、类型、来源和相关度评分。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-B02-3:支持按知识类型、时间范围、来源、知识域等条件过滤。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-B02-4:检索结果受 Domain Purpose(域 M)约束--当知识域配置了目标声明时,检索排序考虑条目与域目标的相关度。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-B02-5:语义检索的存储层实现(sqlite-provider 的 search/findSimilar 等方法)必须走向量检索路径,禁止使用 FTS5 MATCH 做语义检索。FTS5 仅允许用于按 ID 精确查找等非语义场景。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-B02-6:CLI 查询命令(kivo query / kivo search)必须走向量语义检索,禁止使用 FTS5 MATCH 或关键词匹配作为检索实现。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-B02-7:检索结果的相关度评分必须基于向量余弦相似度,禁止使用关键词命中率或 BM25 评分。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。

#### FR-B06:分页查询默认状态过滤

知识分页查询接口默认只返回生效条目,避免治理删除或拒绝的噪音通过遗漏参数重新出现在前端。

- AC-B06-1:`findEntriesPaginated` 默认只返回 status=active 的条目。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-B06-2:调用方只有显式传入 `includeAll=true` 时,才允许返回非 active 条目。该参数仅供治理、审计、迁移和调试入口使用,普通用户页面不得使用。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-B06-3:显式传入 status 过滤条件时,系统按调用方指定状态返回;未传 status 且未传 includeAll=true 时,必须自动补齐 status=active。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-B06-4:所有基于 `/api/v1/knowledge` 的分页接口继承该默认行为。时间线、反向链接、未链接提及、知识联想、快速切换、编辑器关联推荐等调用方即使漏传 status,也只能拿到 active 条目。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-B06-5:返回结果的 total、pageCount、统计摘要必须基于实际过滤后的 active 集合计算,不得把 stale、deleted、rejected 数据计入普通用户视图。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。

#### FR-B03:知识关联

维护知识条目之间的关联关系。

- AC-B03-1:支持的关联类型至少包括:补充(supplements)、替代(supersedes)、冲突(conflicts)、依赖(depends_on)。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-B03-2:新知识入库时自动检测与已有知识的潜在关联。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-B03-3:关联关系可被检索时利用,提升返回结果的完整度。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-B03-4:关联关系同时作为知识图谱(域 G)的边数据,支撑图谱构建和洞察分析。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-B04:语义向量生成与缓存

知识条目入库时自动生成语义向量,支撑语义检索和冲突检测。

- AC-B04-1:知识条目入库时自动触发 Embedding 生成,生成结果与条目关联存储。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-B04-2:Embedding 结果本地缓存,相同内容不重复生成。条目内容变更时自动失效旧缓存并重新生成。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-B04-3:Embedding 生成失败时,条目仍正常入库,标记为"待补建索引",Provider 恢复后自动补建。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-B04-4:语义检索不设降级路径--禁止实现「embedding 不可用时降级为关键词/FTS5/元数据过滤」的逻辑。若 embedding provider 不可达或生成异常,应报错并输出修复引导(检查 provider 连通性、模型是否加载),不降级为非语义检索。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-B04-5:向量索引支持持久化存储(与知识库同一存储引擎),进程重启后自动加载已有索引,不丢失已建向量数据。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-B04-6:向量索引采用内存加速 + 持久化兜底策略--搜索在内存中执行,写入时同步持久化,启动时从持久化存储加载到内存。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。

#### FR-B05:多维知识标签体系(Multi-dimensional Knowledge Tagging)

将知识分类从单一 type 字段升级为三个正交维度,支持多角度检索和组织。

与 FR-B01(结构化知识存储)的关系:FR-B05 扩展 FR-B01 的 schema,新增三个字段,不修改已有字段。FR-B01 的 AC1 中「类型」字段保持不变,FR-B05 在其基础上叠加更细粒度的分类维度。

与 FR-K02(知识分类与路由)的关系:FR-K02 的分类器输出从单一 type 扩展为 type + nature + function + domain 四个字段。FR-K02 的路由逻辑可基于任意维度组合做差异化处理。

三个标签维度:

- **nature**(知识本质):fact / concept / rule / procedure / heuristic。描述知识「是什么形态」。
- **function**(知识功能):routing / quality_gate / context_enrichment / decision_support / correction。描述知识「用来做什么」。
- **domain**(知识领域):开放标签,LLM 自动归类或用户手动指定。描述知识「属于哪个领域」。

向后兼容策略:保留原有 type 字段(fact/methodology/decision/experience/intent/meta),新写入的知识同时填充 type 和三维标签。已有知识的 type 字段不受影响,三维标签字段为空直到执行 retag。

- AC-B05-1:DB schema 新增 nature、function、domain 三个字段,字段类型为字符串,允许为空(向后兼容)。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-B05-2:新写入的知识条目(通过任何入库路径)自动由 LLM 填充三维标签,标签生成集成在知识管线的分类阶段(FR-K02)。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-B05-3:CLI 命令 `kivo retag` 可用,对已有知识条目批量补打三维标签。`--dry-run` 模式预览标签变更(输出每条知识的当前 type 和建议的 nature/function/domain),不写入数据库。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-B05-4:已有知识条目的 type 字段在 retag 过程中不被修改,向后兼容。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-B05-5:`kivo search` 支持按维度组合过滤:`--nature <value>`、`--function <value>`、`--domain <value>`,多个维度参数为 AND 关系。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-B05-6:Web 工作台知识列表(FR-W02)支持按 nature、function、domain 三个维度独立筛选,筛选控件与现有的 type/status 筛选并列。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-B05-7:`kivo retag` 支持 `--limit N` 参数限制单次处理条目数,支持 `--domain <value>` 参数限定 retag 范围。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-B05-8:retag 完成后输出统计报告:处理条目数、各维度标签分布、失败条目数及原因。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。

### 域 C:知识迭代(Knowledge Iteration)

负责知识的更新、冲突解决和一致性维护。

#### FR-C01:冲突检测与解决

新知识与旧知识冲突时自动检测并解决。

- AC-C01-1:系统能检测语义冲突和规则冲突。语义冲突的信号类型包括:同一实体的属性值矛盾、同一决策的结论相反、同一事实的时间版本冲突。规则冲突指同一场景下的矛盾规则。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-C01-2:冲突检测采用两阶段策略--粗筛阶段基于 Embedding 相似度识别候选冲突对(阈值可配置),精判阶段由 LLM 对候选对做语义矛盾判定。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-C01-3:冲突解决策略包括:时间优先(新知识替代旧知识)、来源优先(高权威来源优先)。所有冲突由治理 cron 自动解决,不暴露为用户手动操作。；验收时触发一次计划任务或等效手动运行；日志必须记录开始时间、处理数量、结果状态和失败原因。
- AC-C01-4:冲突解决过程和结论被记录,支持复盘。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-C02:知识过期与清理

自动识别过时知识并处理。

- AC-C02-1:系统能基于时间衰减、引用频率和外部验证标记过时知识。
- AC-C02-2:过时知识直接物理删除,不保留历史。删除前记录快照用于审计追溯。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-C02-3:清理操作生成报告,记录被清理的条目和清理原因。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。

#### FR-C03:知识合并

来自不同来源的同主题知识自动合并。

- AC-C03-1:系统能识别不同来源对同一主题的描述,合并为一条综合知识条目。触发条件:同一主题下存在 2 条及以上无冲突、语义互补的知识条目,且 Embedding 相似度超过可配置阈值。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-C03-2:合并策略按知识类型区分--fact 和 experience 类型取并集合并;decision 类型禁止自动合并,必须人工确认;methodology 类型合并后需人工确认。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-C03-3:合并后的条目保留所有来源引用。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-C03-4:合并操作可回退。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

### 域 D:调研管理(Research Management)

用户通过对话发起调研,Agent 执行并产出报告。Web 页面只做状态查看,不做任务创建。

#### FR-D01:调研任务生命周期

用户在对话中说"帮我调研 XXX",系统自动创建调研任务并追踪到完成。

- AC-D01-1:用户通过对话发起调研(如"帮我调研 XXX"),系统自动创建调研任务,状态为"进行中"。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-D01-2:Agent 执行调研并产出报告,完成后任务状态自动更新为"已完成",失败则标记为"失败"。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-D01-3:每条调研任务记录:主题、状态(进行中/已完成/失败)、创建时间、产出链接(报告文件或飞书文档)。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。

#### FR-D02:调研队列页面(Web)

只读的调研状态查看页面,展示用户发起过的所有调研。

- AC-D02-1:页面以列表形式展示所有调研任务,每条显示主题、状态、创建时间。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-D02-2:已完成的调研任务显示产出链接,点击可跳转到报告或 Wiki 条目。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-D02-3:支持按状态筛选(全部/进行中/已完成/失败)。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-D02-4:页面无任何创建、编辑或配置入口,纯查看。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

### 域 E:意图理解增强(Intent Enhancement)

负责从对话中自动检测意图信号、提取结构化意图知识,并在任务执行时注入相关知识,提升 Agent 对用户意图的理解。覆盖完整的意图知识生命周期:感知 → 提取 → 存储 → 匹配 → 注入 → 治理。

#### FR-E01:上下文注入

Agent 处理用户请求时,自动注入相关知识作为上下文。

- AC-E01-1:注入的知识按相关度排序,总量控制在 token 预算内。token 预算由调用方在检索请求中指定。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-E01-2:注入内容包含知识正文和来源标注,Agent 可判断可信度。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-E01-3:注入过程对用户透明,不改变用户原始请求。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-E01-4:术语注入优先级高于一般知识注入--当预算不足时,优先保障术语注入。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-E01-5:知识注入的检索环节必须使用向量语义检索(BGE embedding cosine similarity),禁止使用 FTS、关键词匹配、正则表达式或规则引擎作为注入检索手段。无例外、无 fallback、无降级。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-E01-6:知识注入的相关性评分必须基于向量余弦相似度计算,禁止使用关键词命中率、TF-IDF、BM25 或任何基于词频的评分机制。relevance-scorer 组件的评分逻辑只允许使用 BGE embedding cosine similarity。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。

#### FR-E02:意图消歧

利用历史知识辅助消解用户意图的歧义。

- AC-E02-1:当用户输入存在多种理解时,系统基于历史决策和用户偏好知识给出最可能的解释。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-E02-2:消歧结果附带置信度和依据的知识条目。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-E02-3:消歧失败时,系统生成结构化澄清建议而非猜测。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。

#### FR-E03:意图信号自动检测

从 Agent 与用户的对话中自动检测意图信号,识别用户的纠偏、强调、声明、规则和偏好表达。

- AC-E03-1:系统通过 LLM 语义识别(非关键词匹配)检测五类意图信号:纠偏(correction)、强调(emphasis)、声明(declaration)、规则(rule)、偏好(preference)。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-E03-2:每个检测到的信号包含信号类型、置信度评分、原始对话片段引用。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-E03-3:置信度低于可配置阈值的信号被丢弃,不进入后续提取流程。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-E03-4:信号检测在对话结束后异步执行,不阻塞 Agent 主任务。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-E03-5:渐进式披露--L0(安装即用):默认启用所有信号类型,阈值 0.6,用户无需配置;L1(调阈值):用户可调整检测灵敏度、启用/禁用特定信号类型、控制每轮对话最大信号数;L2(自定义信号类型):用户可注册自定义信号类型,提供类型名称、描述和检测 prompt 片段,自定义类型走同一条提取管线。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-E03-6:单 Agent 场景兼容--当 OpenClaw 环境只有一个 Agent 时,从主会话对话中收集信号,不依赖子 Agent 事件。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-E04:意图知识提取与结构化存储

将检测到的意图信号提取为结构化意图知识条目,持久化存储并建立向量索引。

- AC-E04-1:信号检测和知识提取合并为一次 LLM 调用,同时输出信号类型和结构化知识,避免多次 LLM roundtrip。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-E04-2:提取的意图知识包含:标题、正文内容、正例列表(用户想要的)、负例列表(用户不想要的)、来源对话引用。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-E04-2.1:每条意图知识条目附带 5~10 条相似句(paraphrases),即用户可能用来表达同一意图的不同自然语言句子。相似句由 LLM 在提取时同步生成,存储在 similar_sentences 字段(JSON 数组)。相似句用于提升意图匹配的召回率--向量检索时同时匹配 content 和相似句。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-E04-2.2:对已有意图条目(similar_sentences 为空),提供 CLI 命令(kivo enrich-intents)批量调用 LLM 补充生成相似句。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-E04-2.3:Web 工作台的知识列表和详情页展示意图条目的相似句,列表页用标签样式,详情页用编号列表。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-E04-3:提取结果以 Knowledge Entry(type=intent)形式入库,走标准知识管线(冲突检测 → 合并检测 → 入库)。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-E04-4:入库的意图知识条目自动生成语义向量并写入持久化向量索引,进程重启后索引自动恢复。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-E04-5:支持批量补漏--定时扫描历史对话日志,对未处理的对话片段做批量信号检测和提取。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。

#### FR-E05:任务派发时意图知识注入

在 Agent 处理用户请求或派发子任务时,自动匹配并注入相关意图知识。

- AC-E05-1:Agent 构建 prompt 前,系统用当前消息内容做向量匹配,将相关意图知识注入上下文,注入内容用标记包裹与其他注入隔离。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-E05-2:派发子任务时,系统用任务描述做向量匹配,将相关意图知识追加到任务 prompt 中。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-E05-3:注入量受 token 预算控制(可配置),预算不足时按相关度排序截断,意图知识优先级高于一般知识。；验收时修改配置后重新执行同一操作；新配置在下一次调用生效，返回值或记录字段体现配置变化。
- AC-E05-4:注入内容包含知识正文和来源标注,接收方可判断可信度。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-E05-5:注入最低相关度分数可配置,低于阈值的匹配结果不注入,避免噪声。；验收时修改配置后重新执行同一操作；新配置在下一次调用生效，返回值或记录字段体现配置变化。
- AC-E05-6:注入路由增加价值判定--向量匹配命中后,系统评估该知识是否填补当前场景下 LLM 的能力盲区(而非仅看语义相似度),只注入 LLM 自身无法覆盖的高价值知识。评估维度:知识的私有性、场景特异性、LLM 在该领域的已知短板。通用常识类知识即使语义匹配度高也不注入(LLM 本来就会的知识只增加 token 消耗,不提升输出质量)。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-E05-7:意图知识注入的检索环节必须使用向量语义检索(BGE embedding cosine similarity),禁止使用 FTS、关键词匹配、正则表达式或规则引擎。无例外、无 fallback、无降级。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-E05-8:每次意图知识注入时记录注入事件(entryId、场景描述、目标 Agent、时间戳),用于意图知识详情页的触发记录展示和列表页的触发频率统计。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

#### FR-E06:意图知识治理

定时对意图知识库执行聚合分析、去重合并和优先级调整,识别高频主题并清理低效条目。

- AC-E06-1:系统定时扫描近 N 天(可配置,默认 7 天)的意图条目,按语义相似度聚类,识别高频主题--被多次独立对话反复强调的规则或纠偏模式。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-E06-2:高频主题自动提升注入优先级权重,提升幅度与出现频率正相关,权重上限可配置。；验收时修改配置后重新执行同一操作；新配置在下一次调用生效，返回值或记录字段体现配置变化。
- AC-E06-3:语义重复的意图条目自动合并为一条,合并策略:保留正例/负例集合的并集,内容取最完整版本,来源引用全部保留。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-E06-4:长期未被触发(连续 N 天未命中注入,N 可配置)的意图条目自动降低优先级权重;超过衰减周期仍未触发的条目直接删除。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-E06-5:每次治理运行后生成治理报告,包含:新识别高频主题数、合并条目数、权重提升条目数、衰减条目数、标记待清理条目数。报告持久化存储,可在工作台查看历史。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-E06-6:治理频率、聚类相似度阈值、衰减周期、权重提升系数均通过配置文件调整,支持运行时热更新。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-E06-7:治理操作全部可回退--合并可拆分还原,权重调整可手动覆盖,待清理标记可撤销。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-E07:意图知识独立路由与检索

意图知识与领域知识(Wiki)在 API 路由、检索接口和前端卡片结构上完全分离,互不干扰。

- AC-E07-1:意图知识拥有独立的 API 路由前缀(/api/intent),与领域知识 API(/api/wiki)完全隔离。两套 API 独立演进,不共享 controller 或 handler。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-E07-2:意图知识拥有独立的语义检索接口,检索范围仅限 type=intent 的条目。领域知识检索接口的范围仅限 domain 标记的条目。两个检索接口独立调用,不存在混合检索模式。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-E07-3:前端意图知识列表和详情使用独立的卡片结构(展示正例/负例/相似句/触发场景),与领域知识卡片结构(展示概念/关系/难度/来源材料)完全不同,不共享组件。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-E07-4:知识注入时(FR-E05),意图知识和领域知识分别独立检索、独立评分、独立注入,注入结果在 prompt 中用不同标记区分。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-E07-5:导航层面,意图知识库和领域知识库作为一级导航的独立入口,互不嵌套,不存在从一方跳转到另一方的隐式路由。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

### 域 F:规则订阅与分发(Rule Subscription & Distribution)

负责统一管理系统规则,按需分发给各 Agent。规则分发通过 API/CLI 管理,不提供独立用户界面。

#### FR-F01:规则注册与管理

统一注册和管理系统规则。

- AC-F01-1:规则条目包含规则内容、适用范围、优先级、生效时间和失效条件。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-F01-2:规则支持版本管理,变更历史可追溯。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-F01-3:规则之间的优先级和覆盖关系被显式管理。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-F02:订阅机制

Agent 按需订阅相关规则。

- AC-F02-1:Agent 可按角色、域、场景订阅规则集。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-F02-2:订阅关系变更时,系统自动调整分发范围。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-F02-3:Agent 可查询自己当前订阅的规则清单。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-F03:规则分发

规则变更时按订阅关系推送给相关 Agent。

- AC-F03-1:规则变更后,Agent 在下一次启动或主动查询时获取最新版本;高优先级规则变更主动通知订阅者。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-F03-2:分发记录可追溯,能确认哪些 Agent 已收到哪个版本的规则。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-F03-3:分发失败时自动重试,超过重试次数后报警。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。

### 域 G:知识图谱与洞察(Knowledge Graph & Insights)

基于知识条目之间的关联关系构建知识图谱,提供结构化洞察和可视化探索能力。这是 KIVO 从"存储知识"到"理解知识结构"的关键跃迁。

#### FR-G01:图谱构建

基于 Knowledge Entry 的关联关系自动构建和维护知识图谱。

- AC-G01-1:图谱节点对应 Knowledge Entry,边对应关联关系(supplements / supersedes / conflicts / depends_on)。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-G01-2:图谱支持多种关联信号源:显式关联(FR-B03 维护的关联关系)、来源共现(来自同一文档或对话的条目)、语义邻近(Embedding 相似度超过阈值的条目)。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-G01-3:新知识入库或关联关系变更时,图谱增量更新,不需要全量重建。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-G01-4:图谱数据通过 API 暴露,支持 Workbench 可视化和外部消费。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-G02:图谱洞察

基于图谱结构自动发现知识组织中的模式和问题。

- AC-G02-1:识别孤立节点--已入库但几乎不参与任何关联的知识条目,提示可能的关联缺失或低价值条目。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-G02-2:识别桥接节点--连接多个主题群的关键知识条目,标注其重要性等级。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-G02-3:识别稀疏社区--条目数量不少但互相支撑关系弱的主题群,提示需要补充关联或深化研究。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-G02-4:发现意外关联--跨主题的非显然关联,基于 Adamic-Adar 共同邻居算法或类型亲和度计算。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-G02-5:洞察结果可作为用户发起调研的参考依据(与域 D 联动),用户可从洞察中识别值得深入研究的方向。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。

#### FR-G03:图谱可视化

在 Workbench 中提供交互式知识图谱浏览。

- AC-G03-1:图谱以力导向布局展示,节点按知识类型着色,边按关联类型区分样式。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-G03-2:支持缩放、平移、节点拖拽和聚焦(点击节点高亮其直接关联)。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-G03-3:支持按知识域、知识类型、时间范围过滤图谱展示范围。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-G03-4:节点点击展示知识条目预览卡片,支持跳转到条目详情页。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-G03-5:洞察结果(孤立节点、桥接节点、稀疏社区)在图谱上以视觉标记高亮。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-G04:知识图谱自动对齐

entries 表发生变更(新增/修改/删除)后,图谱通过定时任务自动增量更新,确保 graph_nodes 和 graph_edges 与 entries 保持一致。与 FR-G01 AC3 的事件驱动增量更新互补--cron 作为兜底机制,覆盖事件遗漏或批量导入场景。

- AC-G04-1:系统配置 cron 定时任务,每天执行一次图谱增量构建(`kivo graph build` 或等效增量命令),调度时间排在知识提取 cron(FR-A01 session 提取)之后。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-G04-2:增量构建只处理上次构建以来发生变更的 entries(基于 updated_at 时间戳或变更标记),不全量重建。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-G04-3:新增 entry 后,下一个 cron 周期内 graph_nodes 自动包含该 entry 对应的节点。；验收时触发一次计划任务或等效手动运行；日志必须记录开始时间、处理数量、结果状态和失败原因。
- AC-G04-4:删除 entry 后,下一个 cron 周期内对应的 graph_nodes 和关联 graph_edges 自动清理。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-G04-5:cron 执行结果写入日志,包含本次处理的 entries 数量、新增/删除的节点和边数量、执行耗时。异常时输出错误信息但不中断后续 cron 任务。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-G04-6:`kivo init` 自动注册图谱重建定时任务到 OpenClaw 内置调度(写入 cron 配置或等效调度机制),用户无需手动配置 crontab。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-G04-7:`kivo init` 注册调度前检查是否已存在图谱重建任务(幂等性),已存在时跳过注册并输出提示,避免重复注册。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-G04-8:图谱重建频率通过配置文件 `graph.rebuildSchedule` 字段管理,默认值为每天一次(`0 3 * * *`),用户可修改为任意 cron 表达式。`kivo init` 注册调度时读取该配置。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。

优先级:P1

#### FR-G05:知识注入图谱关联增强

向量检索命中 top N entries 后,通过图谱边关系扩展关联 entries,使知识注入不仅覆盖语义相似的条目,还覆盖结构关联的条目。

- AC-G05-1:知识注入流程在向量检索(FR-E01)返回 top 10 结果后,对每个命中的 entry 查询 graph_edges,找到 weight 超过配置阈值的强关联 entries。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-G05-2:图谱扩展的 entries 与向量检索结果去重后合并,数量由权重阈值动态决定--超过阈值的全部返回,低于阈值的不返回。设置防爆上限(默认 20)防止极端情况撑爆 context,但正常情况下数量完全按需。禁止硬编码固定返回数量。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-G05-3:图谱扩展的 entries 按 edge weight 降序排列,优先注入关联强度最高的条目。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-G05-4:注入结果中标注每条 entry 的来源路径(vector_match 或 graph_expansion),便于调试和可观测。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-G05-5:图谱扩展的 weight 阈值可通过配置调整,默认值基于当前图谱 edge weight 分布的 P75 值。；验收时修改配置后重新执行同一操作；新配置在下一次调用生效，返回值或记录字段体现配置变化。
- AC-G05-6:当图谱数据不可用(graph_edges 为空或查询失败)时,降级为纯向量检索结果,不阻断注入流程。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-G05-7:`kivo init` 自动将包含图谱关联增强逻辑的知识注入 hook 安装到 OpenClaw hooks 目录。hook 文件为薄壳(符合 §12.1 薄壳约束),核心逻辑通过 `require('@self-evolving-harness/kivo')` 调用包 API。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-G05-8:安装的 hook 同时覆盖向量检索注入和图谱扩展注入,用户无需分别配置两个 hook。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-G05-9:`kivo init` 安装 hook 前检查目标目录是否已存在同名 hook(幂等性),已存在时对比版本--版本更新则覆盖并提示,版本相同则跳过。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。

优先级:P1

#### FR-G06:图谱模式扩展(Graph Schema Extension)

允许外部产品通过 API 或配置文件注册自定义的节点类型和受控关系类型,使 KIVO 图谱在保持通用能力的同时支撑垂直领域的结构化建模需求。未注册模式时,图谱行为与当前一致(自由关系提取);注册模式后,LLM 提取和可视化渲染受模式约束。

- AC-G06-1:支持注册自定义节点类型(Node Type Schema)。每个节点类型包含:类型名称(唯一标识)、属性定义(字段名、字段类型、是否必填)、显示提示(图标、颜色)。注册后,符合该类型的 entries 在图谱中按类型归类。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-G06-2:支持注册受控关系类型(Relation Type Schema)。每个关系类型包含:关系名称(唯一标识)、方向性(有向/无向)、约束条件(允许的源节点类型和目标节点类型组合)。注册后,该类型的关系在图谱中作为受控边存在。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-G06-3:注册的模式对图谱构建有约束力。LLM 提取关系时,若当前图谱已注册模式,提取 prompt 中注入已注册的节点类型和关系类型定义,引导 LLM 优先识别和产出符合模式的结构化关系。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-G06-4:图谱可视化支持按模式渲染。不同节点类型按其注册的图标和颜色显示;不同关系类型按其语义用不同线型或颜色区分。未归类到任何注册类型的节点和边保持默认样式。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-G06-5:模式注册通过 REST API 或配置文件完成,不需要修改代码。API 端点支持模式的增删改查;配置文件方式支持在 KIVO 初始化时批量加载模式定义。；验收时修改配置后重新执行同一操作；新配置在下一次调用生效，返回值或记录字段体现配置变化。
- AC-G06-6:未注册任何模式时,图谱行为退化为当前默认行为(自由关系提取、统一节点样式),不影响已有功能。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-G06-7:多个模式可以共存于同一个图谱。不同模式注册的节点类型和关系类型在同一图谱中并行生效,互不干扰。同一个 entry 可以同时属于通用节点和某个模式定义的节点类型。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-G06-8:模式注册具备校验机制。注册时校验类型名称唯一性、约束条件引用的节点类型是否已注册;校验失败返回明确错误信息,拒绝注册。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-G06-9:模式变更(新增类型、修改约束)不触发已有图谱数据的自动迁移,仅影响后续的图谱构建行为。需要对存量数据应用新模式时,通过手动触发重建完成。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-G06-10:Web 端图谱页面提供一级可达的模式切换入口。用户可在「全部」「通用」和各已注册模式之间即时切换视图,切换不跳页,图谱就地重新渲染对应模式的子图。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-G06-11:新建知识条目时,若当前图谱已注册模式,提供节点类型选择器。选择器展示所有已注册的节点类型(含默认的「通用」类型),用户可一键指定该条目的节点类型。不选择时默认为通用节点。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-G06-12:图谱页面的模式切换入口在侧边栏或工具栏中常驻可见,不藏在二级菜单或设置页。首次进入图谱页面时,若存在多个模式,默认展示「全部」视图。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-G06-13:知识入库时,若图谱存在已注册模式,LLM 自动判断该条目最匹配的模式和节点类型,无需用户手动选择。判断依据为条目内容与模式定义的语义匹配度。匹配不到任何模式时归入通用节点。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-G06-14:学科参考资料采用统一的学术模式(通用素材类型:概念、定理、公式、例题、证明方法、应用场景),学科本身通过 domain 标签区分(如概率论、线性代数)。新学科无需预注册,LLM 从内容语义自动识别学科并赋予 domain 标签,首次出现时自动创建。只有通过 FR-A07 与 FR-N05 的高价值提炼信息,才作为图谱中的 Knowledge Entry 节点。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-G06-15:治理层定期检查学科分类一致性:同一知识点是否被打了不一致的 domain 标签,跨学科关系是否合理。检测到不一致时生成治理建议,不自动修改。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。

优先级:P1

### 域 H:系统词典(System Dictionary)

负责统一管理术语定义,为 Agent 提供术语约束注入,确保全系统术语语义一致。术语条目是 KnowledgeEntry 的一种特化视图,复用已有的存储、检索、冲突检测和生命周期基础设施。术语的主要来源是 KIVO 知识提取管线从会话中自动提取的专有名词和领域术语,手动录入为补充。

#### FR-H01:术语注册与存储

- AC-H01-1:术语条目包含:术语名(term)、英文缩写(abbreviation)、英文全称(fullName)、中文名(chineseName)、定义(definition)、语义约束(constraints)、正例(positiveExamples)、负例(negativeExamples)、适用域(scope)、别名(aliases)。其中 abbreviation、fullName、chineseName 为必填字段。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-H01-2:术语条目以 KnowledgeEntry 形式存储,type 为 fact,domain 为 system-dictionary,结构化字段存放在 metadata 扩展中。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-H01-3:术语名在同一 scope 内唯一,重复注册时拒绝并提示已有条目。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-H01-4:术语检索支持精确匹配模式--术语名或别名完全匹配时直接返回,优先级高于语义相似度排序。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-H01-5:术语的唯一性校验、冲突检测、格式验证均在 API 层执行。

#### FR-H02:术语 Prompt 注入

Agent 执行任务时,自动将相关术语约束注入 prompt 上下文。

- AC-H02-1:注入格式包含术语名、定义、约束、正例和负例。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-H02-2:注入时按任务上下文的语义相关度筛选术语(向量语义检索),不全量灌注。禁止使用 FTS、关键词匹配、正则表达式筛选术语。术语搜索的相关性评分必须基于向量余弦相似度,禁止关键词命中率评分。term-search 组件的评分逻辑只允许使用 BGE embedding cosine similarity。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-H02-3:术语注入的 token 开销纳入总 token 预算管理,术语注入优先级高于一般知识注入。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-H02-4:注入内容支持 markdown 和 plain text 两种格式。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-H02-5:已废弃的术语直接从知识库删除;删除前自动创建替代术语条目(包含旧术语名作为别名),确保 prompt 中出现旧术语名时能通过替代条目注入迁移指引。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。

#### FR-H03:术语冲突检测

- AC-H03-1:冲突类型包括:语义矛盾、范围重叠(scope 交集内 constraints 不兼容)、别名冲突。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-H03-2:语义矛盾检测复用冲突检测管线(embedding 粗筛 + LLM 精判),术语域的粗筛阈值可独立配置。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-H03-3:别名冲突检测为本地精确匹配,不依赖 LLM。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-H03-4:冲突检测结果包含冲突类型、冲突对和建议解决方式。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。

#### FR-H04:术语生命周期

- AC-H04-1:术语生命周期操作包括:新增、修改、废弃、合并。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-H04-2:definition 或 constraints 字段变更触发新版本;aliases、examples、scope 变更为 metadata 更新,不触发版本递增。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-H04-3:废弃术语复用知识过期与清理流程(FR-C02)。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-H04-4:合并术语时,被合并条目状态变为 superseded 并指向合并后的条目,操作可回退。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-H04-5:术语变更生成变更事件,推送给订阅了该术语所属 scope 的 Agent。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。

#### FR-H05:术语来源与批量导入

- AC-H05-1:支持从 JSON、YAML、CSV 格式文件批量导入术语,导入时逐条执行冲突检测。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-H05-2:支持从治理文件中解析术语定义并导入,作为文档知识提取(FR-A02)的一种特化策略。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-H05-3:批量导入生成导入报告,记录成功条数、冲突条数、跳过条数和失败原因。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-H05-4:支持批量导出为 JSON/YAML/CSV,导出格式与导入格式对称。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-H05-5:知识提取管线(域 A)在对话提取过程中自动识别专有名词和领域术语,符合术语结构的候选自动写入系统词典。这是系统词典的主要内容来源,手动录入为补充。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。

### 域 I:OpenClaw 集成层(OpenClaw Integration)

负责 KIVO 引擎与 OpenClaw 宿主环境之间的能力声明、运行时集成和插件对接。

#### FR-I01:OpenClaw 能力声明与注册

- AC-I01-1:OpenClaw 通过集成接口声明自身能力(LLM 调用、网络访问、文件读写、工具执行),KIVO 据此决定可激活的功能域。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-I01-2:能力声明包含能力标识、版本约束和调用约定。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-I01-3:OpenClaw 能力在运行期间变更时,KIVO 收到通知并降级受影响的功能,不中断整体服务。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-I02:LLM Provider 管理

- AC-I02-1:支持注册多个 LLM Provider,每个 Provider 声明支持的能力(文本生成、Embedding 生成、结构化输出)。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-I02-2:KIVO 按任务类型选择匹配的 Provider,选择策略可配置。；验收时修改配置后重新执行同一操作；新配置在下一次调用生效，返回值或记录字段体现配置变化。
- AC-I02-3:当前 Provider 不可用时自动切换到备选 Provider;无可用 Provider 时依赖 LLM 的功能进入降级状态。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。

#### FR-I03:意图管线 OpenClaw 集成

KIVO 通过 OpenClaw 的插件/扩展机制将意图知识管线接入运行时,实现对话流自动采集和知识自动注入。

- AC-I03-1:KIVO 提供插件接口,通过注册 OpenClaw 生命周期钩子(对话结束、prompt 构建前、任务派发前)接入意图管线。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-I03-2:插件采用 fail-open 设计--插件加载失败或 hook 执行异常时,OpenClaw 核心功能不受影响,错误仅记录日志。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-I03-3:插件提供一键安装命令,自动注册到 OpenClaw 配置、写入默认参数,用户只需重启 OpenClaw 即可生效。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-I03-4:插件配置支持热更新--修改阈值、token 预算等参数后无需重启,下次 hook 触发时自动读取新配置。；验收时修改配置后重新执行同一操作；新配置在下一次调用生效，返回值或记录字段体现配置变化。

#### FR-I05:定时任务 CLI 管理(Cron Task Management)

提供 CLI 工具管理 KIVO 的定时任务(图谱重建、知识提取等),支持查看、修改调度频率和手动触发。

- AC-I05-1:提供 CLI 命令 `kivo cron install`,将图谱重建任务注册到系统 crontab(或用户级 crontab),频率读取配置文件 `graph.rebuildSchedule`(默认每天一次)。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-I05-2:`kivo cron install` 支持 `--schedule <cron-expression>` 参数覆盖配置文件中的频率设置。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-I05-3:`kivo cron install` 执行前检查是否已存在同名 cron 条目(幂等性),已存在时更新频率而非重复添加。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-I05-4:提供 `kivo cron uninstall` 命令,清除已注册的 KIVO 相关 cron 条目。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-I05-5:`kivo cron status` 输出当前已注册的 KIVO cron 任务列表、下次执行时间和上次执行结果。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-I05-6:注册的 cron 条目包含标识注释(如 `# KIVO: graph-rebuild`),便于识别和管理。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。

优先级:P1

### 域 K:知识管线编排(Knowledge Pipeline Orchestration)

#### FR-K01:管线阶段编排

- AC-K01-1:对话提取管线精简为五步:原子分解+去上下文化(一步完成) → 行为变化测试+三重测试(准入门禁 FR-A07) → 0.92 cosine 去重 → 素材暂存(staging) → 抽象聚合(产出知识入库)。文档提取管线保留完整阶段(解析 → Reference Material 生成 → 分析中间产物生成 → 分类 → 候选知识萃取 → 冲突检测 → 合并检测 → 质量门禁 → 入库)。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-K01-2:阶段之间通过事件驱动流转,支持异步执行。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-K01-3:任一阶段失败时,管线中断并记录失败上下文,不影响其他独立管线实例。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-K01-4:管线支持扩展--新增处理阶段只需注册到管线,不修改已有阶段逻辑。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-K02:知识分类与路由

- AC-K02-1:分类器识别知识条目的类型和所属知识域。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-K02-2:分类结果决定后续处理路径:不同知识类型可应用不同的冲突检测策略和入库规则。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-K02-3:分类置信度低于阈值时,条目标记为待人工确认,不自动进入后续管线阶段。；验收时修改配置后重新执行同一操作；新配置在下一次调用生效，返回值或记录字段体现配置变化。

### 域 L:分析中间产物(Analysis Artifacts)

负责管理知识提取过程中产生的结构化分析结果。提取管线拆成"分析 → 生成"两步,先把语义中间层做扎实,再进入内容生成,提升提取稳定性和可审计性。

#### FR-L01:分析产物生成

知识提取的第一步产出结构化分析结果,而非直接生成知识条目。

- AC-L01-1:分析产物(Analysis Artifact)包含以下结构化字段:sourceId、extractedClaims(提取的断言列表)、entityCandidates(实体候选)、topicCandidates(主题候选)、linkCandidates(关联建议)、conflictCandidates(冲突建议)、gapCandidates(缺口建议)、reviewCandidates(需人工审核的事项)、recommendedResearchQueries(推荐调研问题)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-L01-2:分析产物与来源文档/对话关联,支持溯源。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-L01-3:分析产物持久化存储,不随知识条目生成后丢弃。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。

#### FR-L02:分析产物审核与消费

分析产物作为知识生成的输入,支持人工审核和自动消费。

- AC-L02-1:Workbench 提供分析产物浏览界面,用户可查看每次提取的分析中间结果。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-L02-2:用户可对分析产物中的候选项逐条确认、拒绝或编辑,确认后的候选项进入知识生成阶段。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-L02-3:自动模式下,高置信度的分析产物直接进入知识生成,低置信度的进入审核队列。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-L02-4:分析产物中的 gapCandidates 和 recommendedResearchQueries 作为用户发起调研的参考建议,展示在工作台中供用户决策。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

### 域 M:知识域目标声明(Domain Purpose)

负责为每个知识域定义目标、关键问题和研究边界,指导知识采集和治理方向。解决"知识该不该入库、如何分类、是否值得继续调研"的判断依据问题。

#### FR-M01:域目标配置

每个知识域可配置目标声明,约束该域的知识采集和治理行为。

- AC-M01-1:域目标声明包含以下字段:domainId、purpose(目标描述)、keyQuestions(关键问题列表)、nonGoals(非目标列表)、researchBoundary(研究边界描述)、prioritySignals(高优先级信号列表)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-M01-2:域目标声明通过配置文件或 Workbench 界面管理,支持创建、编辑和删除。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-M01-3:域目标声明变更时生成变更事件,通知依赖该域的模块重新评估。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。

#### FR-M02:域目标驱动的行为约束

域目标声明影响知识提取、检索和调研的行为。

- AC-M02-1:知识提取时,系统参考目标域的 purpose 和 researchBoundary 判断提取内容是否在域范围内,超出范围的内容标记为"域外"不自动入库。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-M02-2:语义检索时,检索排序考虑条目与域目标的相关度--与 keyQuestions 相关的条目排序权重更高。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-M02-3:缺口检测时,系统参考 keyQuestions 评估哪些问题尚未被知识库覆盖,生成缺口报告供用户参考。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-M02-4:用户发起调研任务时,researchBoundary 作为调研范围参考注入任务上下文,防止调研方向发散。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-M02-5:域目标与知识条目的相关度匹配必须使用向量余弦相似度(BGE embedding),禁止使用关键词匹配或正则表达式做域目标约束判断。domain-goal-constraints 组件的匹配逻辑只允许使用向量语义检索。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。

### 域 N:知识治理(Knowledge Governance)

负责知识库的持续治理:准入门禁、语义去重、质量审计、从 badcase 中自动学习、定期存量治理。确保知识库不膨胀、不退化、持续进化。治理通过 cron 自动执行,不提供独立用户界面。

#### FR-N01:知识 MECE 治理

新增知识前自动检测语义重叠,定期扫描去重,审计知识覆盖度盲区。

- AC-N01-1:新知识入库前,系统自动用 BGE 向量匹配已有知识条目,语义相似度超过可配置阈值(默认 0.85)时标记为疑似重复,阻断自动入库并提示合并。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-N01-2:CLI 命令 `kivo deduplicate` 可用,对全库执行语义去重扫描,输出去重报告(重复对列表、相似度分数、合并建议)。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-N01-3:去重报告中每对重复条目附带合并建议(保留哪条、合并策略、合并后内容预览),用户可一键确认合并或忽略。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-N01-4:`kivo deduplicate --auto` 模式下,相似度超过高阈值(默认 0.95)的重复对自动合并,合并操作可回退。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-N01-5:覆盖度审计--系统基于 Domain Purpose(域 M)的 keyQuestions 检查每个知识域是否存在知识盲区,输出覆盖度报告(已覆盖问题 / 未覆盖问题 / 覆盖率百分比)。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-N01-6:语义相似度检测必须使用 BGE 向量 embedding,禁止关键词匹配、正则表达式或规则引擎。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-N01-7:`kivo deduplicate --domain <value>` 支持按知识域限定扫描范围,减少大库扫描耗时。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。

#### FR-N02:Badcase→意图知识自动转化

从用户纠偏、审计发现、复验失败等 badcase 中自动提取真实意图,结构化写入知识库。

- AC-N02-1:系统识别三类 badcase 来源:用户对话中的纠偏(correction 信号)、审计报告中的问题发现、任务复验失败记录。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-N02-2:从 badcase 中通过 LLM 语义提取真实意图,输出结构化意图知识:场景描述、触发条件、预期行为、反模式(用户不想要的行为)。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-N02-3:提取的意图知识以 Knowledge Entry(type=intent)形式入库,走标准知识管线(冲突检测 → 去重检测 → 入库),自动关联已有相关知识条目。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-N02-4:每条从 badcase 转化的知识条目自动标注来源类型(user_correction / audit_finding / verification_failure)和来源日期。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-N02-5:CLI 命令 `kivo learn-from-badcase` 可用,支持 `--source <path>` 指定 badcase 文件或目录,支持 `--dry-run` 预览提取结果不写入。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-N02-6:入库前自动与已有意图知识做语义去重(复用 FR-N01 的 BGE 向量匹配),重复时合并而非新增。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-N02-7:意图提取必须使用 LLM 语义理解,禁止规则引擎或模板匹配。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-N02-8:系统性问题自动识别--当语义相似度超过可配置阈值(默认 0.85,复用 FR-N01 的 BGE 向量匹配)的一组纠偏记录在 N 天内(N 可配置,默认 14 天)累计超过 M 次(M 可配置,默认 3 次),系统自动将其标记为「系统性问题」而非意图知识。判定依据:该规则属于 LLM 通用能力范围内的常识但 Agent 反复违反,说明根因在流水线或架构层面,一条意图知识无法解决。系统生成结构化问题报告(问题描述、纠偏频率、影响范围、建议修复层级),路由到 SEVO spec 待办而非 KIVO 意图库。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-N02-9:系统性问题报告包含路由建议:应修改 SEVO 流水线的哪个阶段,或应优化 KIVO 架构的哪个模块。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。

#### FR-N03:知识质量审计

审计知识库中每条知识的内容质量,标记不合格条目并生成改写建议。

- AC-N03-1:CLI 命令 `kivo audit-quality` 可用,对全库或指定域的知识条目逐条评估内容质量,评估维度:具体性(是否有明确场景)、可执行性(是否能直接指导行动)、场景化(是否绑定触发条件)。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-N03-2:每条知识条目输出质量评分(1-5 分),评分由 LLM 基于三个维度综合判定。评分标准:5=三维度全满足;4=两维度满足;3=一维度满足;2=模糊但有信息量;1=空泛无法指导行动。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-N03-3:评分 ≤ 2 的条目标记为「质量不合格」,系统为每条不合格条目生成改写建议(保留原意的前提下提升具体性和可执行性)。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-N03-4:CLI 命令 `kivo upgrade-quality --batch` 可用,对所有质量不合格条目批量执行 LLM 改写,改写结果以新版本入库(旧版本保留为 superseded),改写操作可回退。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-N03-5:`kivo audit-quality --domain <value>` 支持按知识域限定审计范围。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-N03-6:审计完成后输出质量报告:总条目数、各评分段分布、不合格率、Top-10 最差条目列表。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-N03-7:质量评估必须使用 LLM 语义判定,禁止规则引擎或关键词匹配。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-N03-8:`kivo audit-quality --threshold <N>` 支持自定义不合格阈值(默认 2),低于等于该阈值的条目标记为不合格。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。

#### FR-N04:知识入库价值门禁(Knowledge Intake Value Gate)

入库前评估知识是否填补 LLM 能力与用户预期之间的差距,拦截低价值知识,确保知识库只沉淀 LLM 自身无法覆盖的高价值知识。

核心判定原则:知识条目的价值 = 填补 LLM 能力与用户预期之间的差距。

高价值知识(应入库):用户私有术语/黑话(LLM 不可能知道)、反复出现的 badcase(LLM 知道但做不到)、专业领域定制意图(LLM 理解浅)、容易被 context 稀释遗忘的关键约束、复杂/隐含/间接意图(LLM 容易误判)、用户特有的指代和简写。

低价值知识(不应入库):通用常识(LLM 本来就会)、被反复纠偏的通用规则(说明是系统性问题,该改流水线或架构,不该加知识条目)。

- AC-N04-1:知识条目入库前,系统通过 LLM 评估该条目是否填补 LLM 能力盲区,评估维度:私有性(LLM 训练数据是否覆盖)、场景特异性(是否绑定用户特有上下文)、纠偏频率(是否源自反复出现的 badcase)。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-N04-2:评估结果为「低价值」的条目阻断入库,直接丢弃并附带拒绝理由(如「属于 LLM 通用能力范围内的常识」)。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-N04-3:评估结果为「高价值」的条目正常进入后续管线(冲突检测 → 合并检测 → 入库)。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-N04-4:价值评估必须使用 LLM 语义判定,禁止关键词匹配或规则引擎。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-N04-5:价值评估阈值可配置,支持运行时热更新。；验收时修改配置后重新执行同一操作；新配置在下一次调用生效，返回值或记录字段体现配置变化。
- AC-N04-6:CLI 命令 `kivo audit-value` 可用,对已有知识条目批量执行价值评估,输出报告(高价值/低价值/待定分布、Top-10 低价值条目列表),支持 `--domain <value>` 限定范围。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。

#### FR-N05:统一入库质量门禁(Unified Intake Quality Gate)

所有知识入库路径(批量萃取、实时提取、CLI 录入、API 写入、文档导入)统一经过质量门禁,确保知识库只接收高质量、不重复、不矛盾的知识条目。FR-N01 提供语义去重能力,FR-N04 提供价值评估能力;FR-N05 将去重、价值评估、冲突检测、过期检测整合为统一的入库前置门禁,所有写入路径必须经过此门禁。

- AC-N05-1:向量相似度去重--入库前使用 BGE 向量 embedding 计算候选条目与已有知识的 cosine 相似度,>= 0.92 时判定为重复,重复条目跳过入库(或仅更新已有条目的时间戳)。去重阈值 0.92 通过 kivo.config.json 的 `extraction.dedup.threshold` 配置,支持运行时热更新。去重必须使用 BGE 向量检索,禁止 FTS 或关键词匹配。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-N05-2:LLM 价值评估--对候选条目执行价值判定,临时状态信息、纯操作记录、通用常识等低价值内容直接丢弃,不进入后续管线。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-N05-3:冲突检测--复用 FR-C01 的冲突检测与解决能力,候选条目与已有知识存在事实矛盾时按 FR-C01 定义的策略自动处理(时间优先、来源优先)。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-N05-4:过期检测--候选条目关联的事实发生变化时(如版本号更新、日期过期、状态变更),自动删除关联的旧知识条目,删除前将过期原因写入门禁日志(AC5)。若旧条目有部分仍有效的内容,先提取有效部分生成新条目再删除旧条目。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-N05-5:门禁结果持久化--每次门禁执行的判定结果(通过、拒绝、合并、冲突、过期标记)写入数据库,包含候选条目内容、判定类型、判定理由、执行时间,支持审计查询。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-N05-6:被拒绝的条目写入 rejected log,不进入主知识库。rejected log 支持按时间、拒绝原因、来源路径查询,用于分析入库质量趋势。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-N05-7:门禁对所有写入 entries 表的路径强制生效,不存在绕过门禁的写入方式。领域材料管线、Wiki 管线、文档导入管线、API 写入、CLI 写入、治理聚合写入均无例外。CLI 提供 `--no-quality-gate` 参数用于调试场景,生产环境默认不可用。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-N05-8:门禁前置检查必须区分 Knowledge Entry 与 Reference Material。概念定义、公式、定理、例题、题目解析、原文摘录等参考资料不得进入 entries 表;若调用方试图写入,门禁必须拒绝并返回“参考资料不能作为知识条目入库”的原因。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-N05-9:任何管线写入 entries 表前都必须调用统一质量门禁,包括领域材料管线和 Wiki 准入管线。禁止在 pipeline、repository、API route 或批处理脚本中直接绕过门禁写 entries。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-N06:批量萃取质量治理(Batch Extraction Quality Governance)

对 FR-A05 会话历史知识萃取的产出施加质量门禁,确保批量提取不向知识库灌入低质量条目。

- AC-N06-1:`kivo extract-sessions` 的产出在写入知识库前,逐条经过统一质量门禁(FR-N05)过滤。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-N06-2:质量门禁默认开启。`--no-quality-gate` 参数可关闭门禁(仅用于调试或迁移场景),关闭时输出警告日志。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-N06-3:萃取完成后输出门禁统计日志:萃取总条目数、门禁通过数、拒绝数、合并数,以及各拒绝原因的分布。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-N06-4:门禁拒绝率超过可配置阈值(默认 80%)时,输出告警提示萃取源质量异常,建议检查 session 筛选条件或噪音过滤规则。；验收时修改配置后重新执行同一操作；新配置在下一次调用生效，返回值或记录字段体现配置变化。

#### FR-N07:知识条目命名规范(Entry Title Convention)

知识条目的 title 字段强制遵守长度规范,确保知识库中所有条目名称简洁可读、适合列表展示和图谱节点标签。content 字段不设长度限制。

- AC-N07-1:所有入库路径(批量萃取、实时提取、CLI 录入、API 写入、文档导入)统一校验 title 字段长度不超过 20 字,不合规时阻断入库。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-N07-2:超长 title 自动压缩策略按优先级依次执行--去括号内容 → 去冒号后英文注释 → 截断至 20 字。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-N07-3:压缩后仍超过 20 字时,截断至 19 字并追加省略号("..."),确保最终长度 ≤ 20 字。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-N07-4:content 字段不设长度限制,允许任意长度的知识描述。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-N07-5:提供存量治理脚本 `kivo governance title-normalize`,对已入库的超长 title 执行压缩清洗,输出处理统计(总条目数、压缩数、截断数、无需处理数)。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。

#### FR-N08:知识库定期治理(Periodic Knowledge Governance)

定时自动执行知识库健康维护,检测过时条目、衰减低活跃知识、扫描逻辑矛盾、回收已成为通用常识的条目。准入门禁(FR-N05)管入口质量,定期治理管存量健康--两者共同确保知识库长期不膨胀、不退化。治理的核心动作是聚合 → 抽象 → 规律提炼 → 有效性评估,相似度只是发现候选的手段,不是治理本身。治理的产出是更少、更精、更抽象的条目。

- AC-N08-1:定期治理任务由 cron 自动触发,默认每天执行一次,执行频率可配置。不依赖人工启动,系统安装后自动生效。；验收时修改配置后重新执行同一操作；新配置在下一次调用生效，返回值或记录字段体现配置变化。
- AC-N08-2:过时检测--LLM 逐条评估知识描述的事实状态是否仍然成立(如版本号已更新、工具已废弃、流程已变更),过时条目直接删除并附带过时原因记录。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-N08-3:价值衰减--系统记录每条知识被检索命中的时间戳(最后命中时间),连续 N 天(N 可配置,默认 90 天)未被命中的条目自动标记为低活跃;超过衰减周期(默认 180 天)仍未命中的条目直接删除。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-N08-4:矛盾扫描--对全库知识条目分批执行两两逻辑一致性检测(LLM 判定),发现逻辑矛盾的条目对自动执行冲突解决(复用 FR-C01 的冲突解决机制)。分批策略:按知识域分组,每批不超过 500 条,避免单次执行耗时过长。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-N08-5:常识回收--LLM 重新评估每条知识是否已成为当前模型的通用能力(随 LLM 迭代升级,之前有价值的私有知识可能变成常识)。判定为常识的条目直接删除,附带删除理由「已被 LLM 通用能力覆盖」。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-N08-6:治理结果持久化--每次定期治理的执行结果写入数据库:执行时间、扫描条目数、各操作类型统计(deleted / conflict_detected)、具体操作明细(条目 ID + 操作类型 + 理由)。支持审计查询和趋势分析。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-N08-7:删除操作记录删除原因和操作时间,但不保留条目内容。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-N08-8:CLI 命令 `kivo governance run` 可手动触发一次完整的定期治理流程,支持 `--domain <value>` 限定扫描范围,支持 `--dry-run` 预览治理结果不执行写入。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-N08-9:治理完成后输出治理报告:扫描总条目数、删除数、新发现矛盾数、常识回收数,以及 Top-10 高风险条目列表(即将触达删除阈值的条目)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-N08-10:所有治理判定必须使用 LLM 语义理解,禁止关键词匹配或规则引擎。矛盾扫描的向量预筛选使用 BGE embedding(先用向量找语义相近的候选对,再用 LLM 精判是否矛盾),减少 LLM 调用量。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-N08-11:BGE 向量预筛分批处理--矛盾扫描的 BGE embedding 预筛阶段必须分批执行,避免一次性对全量条目调用 embedding 导致超时中断。具体要求:；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
  - 每批处理不超过 50 条知识条目的 embedding 计算与余弦相似度比对,批次大小可配置。
  - 每批之间设置间隔(默认 500ms,可配置),避免 BGE 服务过载。
  - 单批超时(默认 30s,可配置)不影响后续批次执行:超时批次记录错误日志并跳过,下次治理执行时优先重试失败批次。
  - 扫描进度可恢复:每批完成后将已处理的条目 ID 范围写入检查点(checkpoint),治理任务中断后下次执行从检查点继续,不重复已完成批次。
  - 检查点持久化到数据库,包含:扫描轮次 ID、已完成批次范围、失败批次列表、最后更新时间。
  - 全量扫描完成(所有批次成功或重试耗尽)后清除检查点,下次治理从头开始新轮次。

#### FR-N09:碎片聚合治理(Fragment Aggregation Governance)

定期扫描知识库中的碎片条目,执行聚合 → 抽象 → 规律提炼 → 有效性评估,产出更少、更精、更抽象的高密度知识条目。治理的核心动作是提炼黄金--从碎片中收敛出少量高密度抽象知识,而非简单的相似度去重。

碎片的定义:同主题多条、粒度过细、绑定特定时刻或场景、未经抽象的条目。典型特征:多条条目说的是同一件事的不同侧面,合并后信息密度更高。

- AC-N09-1:定期扫描知识库中的碎片条目。碎片识别策略:用 BGE 向量检索找到语义相近的条目簇(作为候选),再用 LLM 精判这些候选是否属于同主题碎片。禁止使用关键词匹配、正则表达式、FTS5、规则引擎承担语义理解职责。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-N09-2:对碎片执行 LLM 语义聚合--多条碎片 → 一条抽象知识。聚合的目标是提炼跨场景、跨时间的规律和原则,而非简单拼接或取交集。聚合由 LLM 执行,禁止关键词匹配、正则表达式、FTS5、规则引擎。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-N09-3:聚合后的知识条目必须通过 FR-A07 定义的三重测试(时效性、跨场景、抽象性)。不通过的聚合结果不入库,原始碎片保留等待下次治理。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-N09-4:聚合完成后删除原始碎片。删除操作记录聚合来源(哪些碎片 ID 被聚合为哪条新知识),支持审计追溯。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-N09-5:有效性评估必须是设计过的,不是跑个相似度阈值就完事。评估维度:这条知识在当前环境下还成立吗?有没有被更新的理解 supersede?它指导的行为是否仍然正确?评估由 LLM 执行,禁止关键词/正则/规则引擎。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-N09-6:治理频率可配置(默认每天一次,与 FR-N08 定期治理同步执行或独立调度)。CLI 命令 `kivo governance aggregate` 可手动触发,支持 `--domain <value>` 限定范围,支持 `--dry-run` 预览。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-N09-7:聚合治理完成后输出报告:扫描总条目数、识别碎片簇数、聚合产出新知识数、删除碎片数、聚合失败(未通过三重测试)数。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-N09-8:聚合产出的知识条目必须能回答「它让 agent 在什么场景下避免了什么错误」。回答不了的聚合结果不入库。这是目标结果导向约束--知识库和图谱存在的唯一目的是让 agent 做出更好的判断。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-N09-9:禁止硬编码定量。不写死「每次最多聚合 N 条」「相似度阈值 0.X」等魔法数字。聚合的触发和范围由 LLM 语义判断决定。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。

### 域 W:知识工作台(Knowledge Workbench)

面向用户的知识管理 Web UI,是 KIVO 从 API 引擎走向可感知产品的关键层。Workbench 是独立产品对象,围绕四个核心入口构建:意图知识库、领域知识库(LLM Wiki)、知识图谱、知识搜索。全站采用浅色主题(白色背景 + 黑色文字)。

#### FR-W01:运行态概览

在主界面顶部提供知识库运行态指标摘要。

- AC-W01-1:展示核心运行态指标:知识总量、本周新增知识数、知识类型分布、领域知识库数量。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W01-2:指标同时展示绝对值和趋势变化(较上周)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W01-3:所有内部链接使用正确的 basePath 前缀。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-W01-4:仪表盘展示各领域知识库的摘要卡片(库名、条目数、最近更新时间),点击跳转到对应领域知识库详情页。加工链路统计在各领域知识库页面内展示(见 FR-P01 AC10),仪表盘不重复展示管线流程。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W01-5:仪表盘展示的各类知识统计数(意图知识数、领域知识数、wiki 页面数)必须与对应子页面的实际数据一致。统计口径:以 entries 表实际记录为唯一数据源,按 type/domain 字段分类计数,禁止缓存不一致。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W01-6:仪表盘必须展示所有知识类型的统计,包括 wiki 类型,不得遗漏任何已定义的 entry type。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

#### FR-W02:知识列表与搜索

知识条目的浏览、搜索和筛选。包含列表内快速搜索和全站知识搜索页两种场景。

- AC-W02-1:知识列表支持按类型、状态、来源、知识域筛选,支持分页。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W02-2:分页控件展示当前页码、总页数和总条目数。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W02-3:列表内嵌快速搜索,支持在当前筛选结果中按关键词即时过滤。搜索框 placeholder 为“搜索标题、内容、标签”。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W02-4:语义搜索结果展示高亮片段和相关度评分。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W02-5:全站知识搜索页(`/search`)入口位于侧边栏「知识搜索」及全站顶部搜索框(任何页面可用)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W02-6:搜索页搜索框下方展示最近搜索历史(最近 10 条),用户可点击历史记录快速重复搜索。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W02-7:输入时实时展示搜索建议(基于已有知识标题的前缀匹配),用户可点击建议直接跳转。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W02-8:搜索结果分类展示,通过 Tab 切换:「全部」(混合展示按相关度排序)、「领域知识」(仅 Wiki 页面和领域知识条目)、「意图知识」(仅意图类知识)、「材料」(仅原始材料文件)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W02-9:每条搜索结果展示:标题(关键词高亮)、摘要片段(命中内容高亮)、类型标签、所属领域、相关度指示(高/中,不展示具体分数)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W02-10:结果支持按类型、领域、时间范围筛选。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-W02-11:无结果时展示「未找到相关知识」+ 建议操作(上传材料/发起调研)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

#### FR-W03:知识条目详情

展示知识条目的完整信息。

- AC-W03-1:详情页标题区域显示 ≤20 字的短标题,超出截断并显示省略号。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-W03-2:标题下方展示完整知识内容、来源引用、版本历史时间线和关联关系。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W03-3:关联关系以可点击链接展示,支持跳转到关联条目。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W03-4:版本历史支持对比查看不同版本的差异,并支持一键还原到选定版本。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-W03-5:详情页不展示置信度字段。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W03-6:编辑按钮文案为"编辑知识点"。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

#### FR-W04:操作日志

系统行为的客观记录,低频查看的审计入口。

- AC-W04-1:操作日志记录以下事件类型:知识条目变动(创建/删除/合并,含来源标注)、文档导入事件(文件名+参考资料数+Knowledge Entry 数)、调研完成事件(主题+报告路径)、治理运行摘要(合并N条/清理N条)、向量化批次完成。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-W04-2:操作日志支持按事件类型筛选,按日期分组展示,时间格式统一。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W04-3:新事件产生时实时推送到已连接的客户端。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-W04-4:客户端断线重连后自动补发断线期间的事件。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-W06:知识条目操作

用户在 Workbench 中对知识条目执行内容编辑和删除。

- AC-W06-1:支持删除过时的知识条目,删除后不保留历史。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-W06-2:编辑提交后生成新版本,新版本经过冲突检测管线。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-W06-3:编辑操作使用乐观锁(expectedVersion),版本冲突时提示用户刷新后重试。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-W06-4:支持一键还原知识条目到任意历史版本。还原操作本身生成一个新版本(记录"从版本X还原")。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。

#### FR-W07:调研管理

调研任务的查看和管理,是 Wiki 的输入端。

- AC-W07-1:调研任务列表展示任务描述、状态(进行中 / 已完成 / 已归档)、创建时间,支持按状态筛选和分页。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W07-2:每个已完成的调研任务展示产出链接(报告 md 文件路径、飞书文档链接),可直接点击查看。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W07-3:用户可将已完成的调研报告标记为"采纳",触发 Wiki 准入流程(与域 P 联动)。只有用户手动标记"采纳"才触发入库,Agent 不能自动触发入库。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-W07-4:用户可归档已完成的任务,归档后任务从默认列表隐藏(可通过筛选查看)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W07-5:调研报告支持质量标记。Agent 完成调研后可自动标记"重点"(信息密度高、覆盖面广);用户也可手动标记或取消"重点"。"重点"标签仅作为质量参考,不触发入库动作。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-W07-6:调研页面展示真实调研数据。数据来源为系统实际执行的调研任务记录(存储在 DB 中),禁止使用写死的 demo 数据。每次调研任务完成时自动写入一条记录。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

#### FR-W08:材料库与文档导入

Web 端的材料管理和文档导入界面,覆盖材料列表、上传、详情、进度展示和操作管理。

- AC-W08-1:支持上传 PDF、图片、文本/Markdown、音频、视频格式文件,单文件上限 50 MB。支持拖拽上传和点击上传两种方式。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-W08-2:上传时支持选择目标领域(默认「自动识别」,也可手动选择已有领域)。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-W08-3:材料卡片展示人话处理阶段进度:已登记 → 识别材料 → 提取内容 → 形成参考资料 → 萃取知识 → 归入领域 → 生成 Wiki → 建立关系。当前步骤高亮,已完成步骤打勾。长文档额外展示已处理片段数/总片段数。不展示 embedding、worker、cron 等内部技术概念。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W08-4:处理完成后卡片展示产出统计(参考资料 N 条、Knowledge Entry N 条、Wiki 页面 N 个、关系 N 条),并提供三个跳转入口:「查看产出的内容」、「进入领域知识库」、「打开 Wiki 页面」。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W08-5:材料详情页展示:文件摘要(文件名、类型、大小、上传时间、处理状态、所属领域)、产出统计(切片数、参考资料数、Knowledge Entry 数、Wiki 页数、关系数)、内容列表(按类型分组,每条显示标题、摘要、来源定位)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W08-6:材料详情页提供以下操作:「重新处理」(重新触发提取管线)、「删除材料」(确认对话框说明影响)、「查看原文」(打开材料阅读器)。每条 Reference Material 或 Knowledge Entry 可「编辑」「删除」「查看在 Wiki 中的位置」。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-W08-7:材料列表支持多选批量操作(批量删除、批量重新处理)。列表支持按状态(全部/处理中/已完成/失败)、按领域筛选,支持按上传时间、状态、领域排序。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W08-8:上传失败时卡片显示失败原因(文件太大/格式不支持/网络失败),提供「重试」按钮。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W08-9:空状态展示引导文案「上传文档、图片、音频或视频,系统自动解析资料并萃取高价值知识」,附带上传按钮。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W08-10:材料详情页支持手动编辑材料正文内容。编辑保存后触发重新解析参考资料并萃取知识候选(调用已有 pipeline)。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-W08-11:材料详情页支持添加、编辑、删除批注。批注是用户的个人标注,不触发重新提取。批注列表展示在材料正文下方,每条显示内容和时间。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

#### FR-W09:系统字典管理

术语的浏览、新增、编辑和删除。

- AC-W09-1:术语列表支持搜索和按 scope 筛选。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W09-2:新增表单默认收起,按需展开。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-W09-3:删除操作弹出确认对话框。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-W09-4:支持批量导入和批量导出。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-W11:登录与身份

- AC-W11-1:CTA 按钮与背景的对比度满足 WCAG AA 标准(≥ 4.5:1)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W11-2:用户界面不出现开发者术语。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-W11-3:单用户密码登录,无需用户名。登录后保持会话直到主动退出或会话超时。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-W12:导航与页面发现

- AC-W12-1:完成首次 onboarding 后,后续访问默认进入意图知识库。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-W12-2:一级侧边栏导航结构(从上到下):仪表盘、意图知识库、领域知识库(LLM Wiki)、知识搜索、知识图谱、材料库、调研队列、系统(含操作日志)、设置。各入口有固定导航位置。治理、审阅、规则功能作为后台自动化能力存在(cron/API),不提供独立 UI 页面。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W12-3:意图知识库和领域知识库(LLM Wiki)在导航、页面结构、API 路由上完全独立。两者不共享列表页、详情页或卡片组件,各自拥有独立的 URL 路径前缀(/intent、/wiki)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

#### FR-W14:知识列表状态过滤

知识列表展示所有活跃条目,不存在归档概念。

- AC-W14-1:知识列表页展示所有 status=active 的条目。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W14-2:列表页展示条目总数。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W14-3:所有前端知识展示视图默认只展示 active 条目,包括时间线、知识联想、反向链接、未链接提及、快速切换、编辑器关联推荐、图谱节点详情和搜索结果。任何视图需要展示非 active 数据时,必须显式使用管理员/审计入口,并在页面上标明数据状态。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

#### FR-W15:知识列表结构化布局

知识列表以紧凑表格布局展示,不使用卡片样式,每条知识的关键字段一目了然。

- AC-W15-1:列表每行展示以下核心列:type(知识类型,tag 样式)、title(知识名称,≤20字截断)、content(内容预览,单行截断,hover 展示全文)、status(状态)、date(创建/更新日期)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W15-2:字段超长时截断并展示省略号,hover 可查看完整内容。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W15-3:列表支持按各字段列头排序(至少支持 date、type)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W15-4:列表页不展示置信度字段。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W15-5:响应式布局--窄屏下自动折叠低优先级列(domain、status),保留 title、content、date。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。

#### FR-W16:知识图谱导航与稳定性

图谱交互的导航闭环和异常恢复,确保用户不会因交互操作陷入死胡同或触发加载失败。

- AC-W16-1:点击节点展开详情后,界面提供明确的「返回全局视图」按钮,一键回到图谱全貌。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W16-2:连续多次点击节点不会导致图谱加载失败--实现防重复请求机制(debounce 或请求取消),同一时刻仅保留最新一次请求。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-W16-3:节点展开/收起期间展示 loading 状态指示器,阻止用户在加载完成前触发冲突操作。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W16-4:图谱数据加载失败时展示 retry 按钮和用户可读的错误提示,点击 retry 重新请求数据。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-W16-5:retry 失败超过 3 次时,提示用户刷新页面并记录错误日志供排查。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

### 域 X:访问控制与可观测性(Access Control & Observability)

#### FR-X01:知识域访问控制

- AC-X01-1:每条知识条目归属一个知识域,检索请求携带 callerRole,系统仅返回该角色有权访问的域内条目。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-X01-2:域-角色映射通过配置文件静态定义(初期),支持后续升级为动态权限管理。；验收时修改配置后重新执行同一操作；新配置在下一次调用生效，返回值或记录字段体现配置变化。
- AC-X01-3:规则分发遵循订阅范围约束,不向未订阅的 Agent 推送超出其域权限的规则内容。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-X01-4:调研任务仅访问任务创建者有权限的信息源。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-X02:核心指标度量采集

- AC-X02-1:采集知识检索命中率--记录每次检索请求的查询内容、返回条目数和是否命中,支持按时间窗口聚合。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-X02-2:采集缺口检测覆盖率--记录缺口检测运行结果,支持与已知盲区基准对比。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-X02-3:采集规则分发到达率--记录每次分发的目标订阅者数、成功送达数、失败数。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-X02-4:采集知识冲突解决率--记录冲突产生数、已解决数。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-X02-5:度量数据通过运行态概览接口对外暴露,不引入独立的度量存储。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-X03:知识库批量导出

- AC-X03-1:支持按知识域、知识类型、状态、时间范围筛选导出范围,导出格式为 JSON。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-X03-2:导出内容包含知识条目完整数据(元数据、内容、关联关系、版本历史)和冲突记录。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-X03-3:导出文件包含格式版本号,用于导入时的兼容性校验。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。

#### FR-X04:知识库批量导入

- AC-X04-1:导入前校验文件格式版本兼容性,不兼容时拒绝导入并提示原因。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-X04-2:逐条导入时执行冲突检测,冲突条目提示用户选择保留哪个版本,不静默覆盖。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-X04-3:导入完成后生成导入报告。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-X04-4:导入操作支持干跑模式(dry-run),仅输出报告不实际写入。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

### 域 Z:开箱即用与商用就绪(Out-of-Box & Commercial Readiness)

确保外部用户不读源码也能完成安装、初始化、首次使用和持续运营。

#### FR-Z01:安装与环境校验

- AC-Z01-1:提供至少一种正式支持的安装路径(npm / 源码 / Docker),并声明支持矩阵(Node 版本、OS、架构)。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-Z01-2:安装文档列出所有必需依赖、环境变量和可选依赖及其用途。Embedding provider 列为必需依赖,标注推荐方案(Ollama + bge-m3)和替代方案(OpenAI embedding 等兼容 API)。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-Z01-3:提供 health check 命令,输出缺失项和修复建议。health check 必须覆盖 embedding provider 连通性检测。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-Z01-4:安装成功后给出可验证结果(API 健康检查通过、Web 登录页可访问、示例查询有返回)。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-Z01-5:`kivo init` 必须检测 embedding provider 是否已配置且可达。检测内容:配置文件中 embedding 相关字段是否填写、provider endpoint 是否响应、指定模型是否可用。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-Z01-6:检测不通过时,输出分场景引导信息(不是报错退出,是引导用户操作):未配置 → 引导填写配置(推荐 Ollama 方案:`curl -fsSL https://ollama.com/install.sh | sh` + `ollama pull bge-m3`);已配置但不可达 → 引导检查服务状态;模型未加载 → 引导拉取模型。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-Z01-7:可选 `kivo init --auto-setup` 自动执行推荐方案的安装和配置(安装 Ollama、拉取 bge-m3、写入默认配置),执行前需用户确认。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。

#### FR-Z02:首次运行 Bootstrap

- AC-Z02-1:首次启动自动检测是否已完成初始化,未初始化时进入引导流程。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-Z02-2:引导流程覆盖管理员账号创建、存储路径确认、LLM Provider 配置校验、Embedding Provider 配置校验。Embedding Provider 校验为阻断项--未通过则 bootstrap 不完成。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-Z02-3:支持导入示例数据或术语种子数据,用户可跳过。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-Z02-4:初始化完成后自动引导用户跑通"导入一条知识 → 检索命中"演示路径。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-Z02-5:Embedding Provider 配置引导包含:provider 类型选择(Ollama / OpenAI / 自定义兼容 API)、endpoint 地址、模型名称。选择 Ollama 时额外检测本地 Ollama 服务状态和模型可用性。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。

#### FR-Z03:最小运行模式

- AC-Z03-1:定义最小运行组合:1 个 LLM Provider、SQLite、本地 Web,无需外部服务。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-Z03-2:Embedding Provider 是强制依赖。未配置或不可达时,依赖向量检索的功能直接报错并输出修复引导(provider 配置方法 + 推荐方案),不降级为关键词检索。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-Z03-3:无 OpenClaw 宿主环境时,调研任务可创建和排队,支持人工触发执行。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-Z03-4:最小运行模式下所有核心能力可用;OpenClaw 插件集成后通过能力声明接口扩展能力集(如调研执行、定时任务等)。；验收时触发一次计划任务或等效手动运行；日志必须记录开始时间、处理数量、结果状态和失败原因。

#### FR-Z04:配置与密钥管理

- AC-Z04-1:提供带注释的示例配置文件,包含字段说明、类型和默认值。；验收时修改配置后重新执行同一操作；新配置在下一次调用生效，返回值或记录字段体现配置变化。
- AC-Z04-2:支持环境变量与配置文件两种注入方式,优先级规则在文档中明确。；验收时修改配置后重新执行同一操作；新配置在下一次调用生效，返回值或记录字段体现配置变化。
- AC-Z04-3:配置校验失败时返回包含原因和修复建议的用户可读错误。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-Z04-4:Web 端提供配置状态检查页,展示各 Provider 连通性和配置完整度。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

#### FR-Z05:用户文档交付包

- AC-Z05-1:发布时必须包含 README、快速上手、配置参考、故障排查、升级说明五份文档。；验收时修改配置后重新执行同一操作；新配置在下一次调用生效，返回值或记录字段体现配置变化。
- AC-Z05-2:每份文档有明确完成标准,发布前逐份验收。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-Z05-3:Quick Start 中所有示例代码纳入自动化测试,保证可运行。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-Z05-4:文档覆盖 CLI 单独使用、OpenClaw 插件集成、Web 工作台三种使用路径。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-Z05-5:README 快速上手章节必须将 embedding provider 列为前置依赖,包含推荐方案(Ollama + bge-m3)的安装命令和替代方案说明。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-Z05-6:30 秒体验路径必须可走通:`npm install → kivo init(自动检测 + 引导配置 embedding provider)→ kivo query`。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。

#### FR-Z06:首次知识旅程

空库用户 10 分钟内看到系统价值。

- AC-Z06-1:空库状态下提供引导入口,至少包含"上传文档""导入示例数据""手动新建知识"三种起步方式。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-Z06-2:完成任一入口操作后,系统引导用户前往知识库查看结果并尝试检索。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-Z06-3:用户首次搜索无结果时,给出下一步操作建议。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-Z06-4:主界面展示系统就绪度检查清单。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

#### FR-Z07:错误提示与恢复动作

- AC-Z07-1:覆盖用户操作全链路的常见失败场景,每类场景有用户可读的错误提示。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-Z07-2:错误提示包含原因说明和建议的下一步动作。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-Z07-3:可重试的失败支持一键重试。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-Z07-4:关键失败场景支持复制诊断信息。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。

#### FR-Z08:认证生命周期

- AC-Z08-1:单用户密码认证,通过 bootstrap 流程设置密码。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-Z08-2:支持密码修改、会话超时自动失效和主动退出。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-Z08-3:操作审计覆盖知识生命周期关键动作。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-Z09:文档与实现一致性门禁

- AC-Z09-1:README 和 Quick Start 中所有示例代码纳入 CI 校验,示例不可运行时构建失败。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-Z09-2:公开 API 签名变更时,文档和示例必须同步更新。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-Z10:升级与数据迁移

从旧版本升级到新版本时,已有数据安全迁移,不丢失、不损坏。

- AC-Z10-1:每次 schema 变更附带迁移脚本,支持从上一个正式版本自动迁移到当前版本。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-Z10-2:升级前自动执行数据完整性校验,校验不通过时中止升级并输出诊断信息。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-Z10-3:升级失败时支持回滚到升级前状态,回滚操作不丢失升级前的数据。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-Z10-4:升级说明文档记录每个版本的 breaking changes、迁移步骤和已知兼容性问题。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-Z11:运行时 Embedding Provider 健康检查

CLI 命令执行前自动检测 embedding provider 连通性,确保向量检索能力可用。

- AC-Z11-1:所有依赖向量检索的 CLI 命令(query、search、extract-sessions、import 等)执行前,自动检测 embedding provider 连通性。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-Z11-2:检测内容:endpoint 可达(HTTP 连通)、模型响应正常(发送测试向量请求)。检测超时阈值可配置,默认 5 秒。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-Z11-3:不可用时输出友好错误信息 + 修复命令,不输出 stack trace。错误信息示例:"KIVO 需要可用的 embedding provider 提供向量检索能力。当前配置:[provider] [endpoint]。请检查服务是否运行:[对应修复命令]"。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-Z11-4:对于 Ollama 类本地 provider,修复命令示例:`ollama serve`(服务未启动)、`ollama pull <model>`(模型未加载)。对于远程 API 类 provider,提示检查网络和 API key。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-Z11-5:健康检查结果缓存 60 秒,避免同一会话内重复检测。缓存失效后下次命令重新检测。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。
- AC-Z11-6:提供 `kivo doctor` 命令,输出完整的环境诊断报告(Node 版本、存储状态、embedding provider 状态、LLM provider 状态),用于故障排查。；验收时执行对应 CLI 或 API 调用，退出码为 0 或返回 2xx；输出必须包含本 AC 描述的字段/统计项，失败场景返回用户可读原因。

### 域 P:领域知识库(LLM Wiki)

负责管理用户定义的领域知识库(如"线性代数""数理统计""产品设计"等学科/领域),支持参考资料归档、Wiki 编译、主题目录浏览和语义检索。领域库中的教材概念、公式、定理、例题、题目解析和原文摘录作为 Reference Material / Wiki 编译素材存储,不混入 entries 表。只有用户主动创建、标记或系统萃取出的高价值提炼信息,并通过 FR-A07 与 FR-N05 后,才作为 Knowledge Entry 写入 entries 表并进入知识图谱。

#### FR-P01:领域知识库管理

用户可以创建、编辑、删除领域知识库。

- AC-P01-1:支持创建领域知识库,每个库包含名称、描述和知识范围定义。名称全局唯一。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-P01-2:支持编辑领域知识库的名称、描述和范围定义。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-P01-3:支持删除领域知识库,删除前提示该库下的知识条目数量,确认后执行。删除库不自动删除其下的知识条目(条目变为未分类)。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-P01-4:领域知识库列表页展示每个库的名称、描述、条目数量和最近更新时间。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P01-5:领域知识库支持树形目录结构。用户可创建多级子目录(如:数学 → 概率论 → 条件概率 → 贝叶斯定理),知识条目挂在叶子节点或任意层级节点下。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-P01-6:知识条目支持多标签。一个知识点可以同时属于多个领域目录节点(如同时挂在"概率论"和"统计推断"下),通过标签实现多维检索筛选。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-P01-7:领域知识库浏览支持按树形路径导航和按标签筛选两种模式。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-P01-8:领域知识库详情页展示"来源材料"区域,列出所有导入到该库的原始文件(PDF/图片/音频/视频),每个文件显示:文件名、导入时间、处理状态(处理中/已完成/失败)、参考资料数量、Knowledge Entry 数量。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P01-9:点击来源材料可查看该文件解析出的参考资料和 Knowledge Entry 列表(正向溯源:从文件到内容)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P01-10:领域知识库页面顶部展示该库的加工链路统计:导入材料数 → 参考资料数 → Knowledge Entry 数 → 图谱节点数 → 注入次数,让用户一眼看到从导入到使用的完整流转。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P01-11:领域知识库的组织单元为 Subject(学科节点)。Subject 收编原有的 Wiki Directory 功能,作为知识条目、材料、Wiki 页面的统一归属容器。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P01-12:Subject 与知识条目、材料、Wiki 页面之间为多对多关系,通过关系表(entry_subjects、material_subjects、wiki_page_entries)关联。一个知识条目可归属多个 Subject,一个 Subject 下可包含多个条目。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P01-13:Subject 支持树形层级结构(父子关系),用户可创建多级 Subject 树。Subject 详情页聚合展示其下所有关联的知识条目、材料和 Wiki 页面。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

#### FR-P02:领域知识条目

每个领域知识库下包含两类对象:参考资料与知识条目。参考资料承载教材概念、公式、定理、例题、题目解析和原文摘录,用于 Wiki 编译、检索和溯源;知识条目只承载能指导决策/行动的提炼信息,支持 LLM 语义检索。两类对象存储、治理和展示口径必须分开。

- AC-P02-1:领域知识条目只有在满足 FR-A07 知识定义并通过 FR-N05 门禁后,才复用 KnowledgeEntry 数据结构,通过 domain 字段归属到对应的领域知识库。参考资料不得写入 entries 表。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-P02-2:领域知识库详情页分别展示参考资料和 Knowledge Entry,支持按类型筛选、分页和语义搜索。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P02-3:支持在领域知识库内手动创建、编辑、删除知识条目。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-P02-4:领域知识的检索必须使用向量语义检索(BGE embedding cosine similarity),禁止关键词匹配。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-P02-5:参考资料支持以下核心类型:concept(概念/定理/公式)、method(解题方法/证明方法)、question(题目/真题/模拟题)、mistake(错题记录,含错因)、annotation(用户批注/笔记)。类型通过 material_type 字段区分。只有 mistake 中的错因总结、用户批注中的方法论、解题策略中的可迁移原则等具备行动价值的内容,才可萃取为 Knowledge Entry。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-P02-6:领域 Knowledge Entry 核心字段:title、aliases(别名列表)、summary(一句话摘要)、body(正文/详细内容)、difficulty(难度 1-5)、importance(重要性:high/medium/low)、properties(类型化扩展字段,如行动场景、适用条件、边界)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P02-7:题目类参考资料(question)额外字段:source(来源:真题/模拟/自编)、answer(标准答案)、solution(解题过程)。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-P02-8:错题类参考资料(mistake)额外字段:original_question(关联原题 ID)、wrong_answer(错误答案)、error_cause(错因分析)、correction(纠正方法)。其中错因分析和纠正方法可作为 Knowledge Entry 候选进入门禁。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-P02-9:参考资料重要性(importance)支持手动设置,也支持系统根据考频、关联题目数量自动推算建议值。自动推算仅作建议,不覆盖用户手动设置。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

#### FR-P03:领域知识与图谱集成

领域 Knowledge Entry 作为节点纳入知识图谱(域 G),与意图知识共享同一个图谱。Reference Material 通过 Wiki/材料关系图组织,不作为 Knowledge Entry 节点。

- AC-P03-1:领域 Knowledge Entry 入库时自动作为节点加入知识图谱,复用域 G 的图谱更新机制。Reference Material 只进入 Wiki/材料关系图,不作为 Knowledge Entry 节点。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-P03-2:图谱中可以看到领域知识与意图知识之间的关联关系。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-P03-3:图谱节点支持按 domain 筛选,可只查看某个领域知识库的子图。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-P03-4:学科知识支持以下关系类型(第一版收敛,不开放自定义):；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
  - prerequisite_of:前置依赖(学 B 之前必须先学 A)
  - explains / illustrates:解释/举例(Knowledge Entry ↔ Reference Material)
  - assesses:考察(题目考察哪些主题或知识)
  - solved_by:解法(知识/题目对应什么解题方法)
  - confusable_with:易混淆(两个主题或知识容易搞混)
  - derived_from / proves:推导/证明关系
  - applies_to:应用场景(理论→实际问题)
  - belongs_to / has_part:归属/包含(章节结构)
  - annotated_with:批注关联(用户笔记↔知识点)
- AC-P03-5:关系带 strength(关联强度 0-1)和 confidence(置信度)属性。LLM 自动生成的关系默认 confidence=0.7,用户手动确认后提升为 1.0。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-P03-6:领域对象详情页展示该对象的所有关联:前置资料、相关参考资料、解题方法、用户批注、易混点,按关系类型分组呈现。页面必须区分参考资料和 Knowledge Entry。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P03-7:检索流程:用户查询 → 向量召回 topK → 图谱扩展一跳(找前置、解法、相关参考资料)→ 按关系类型分组返回。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。

#### FR-P04:领域材料归档与知识萃取

从会话、文档等来源自动识别领域归属,将教材概念、公式、定理、例题、题目解析和原文摘录作为参考资料归入对应领域库。系统不得自动把这些参考资料写入 entries 表。

- AC-P04-1:材料解析管线(域 A)产出的 Reference Material,根据内容语义自动匹配到最相关的领域知识库(基于领域库的范围定义做向量匹配)。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-P04-2:匹配置信度低于阈值时,参考资料归入默认域(未分类),用户可后续手动归类。；验收时修改配置后重新执行同一操作；新配置在下一次调用生效，返回值或记录字段体现配置变化。
- AC-P04-3:用户导入文档时可指定目标领域知识库,解析出的参考资料和 Wiki 编译素材直接归入该库。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-P04-4:领域匹配必须使用向量语义检索,禁止关键词匹配或规则引擎。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-P04-5:材料支持主领域和副领域。导入材料时系统自动匹配一个主领域(置信度最高)和零到多个副领域(置信度次高),参考资料同时归入主领域和副领域对应的知识库。用户可手动调整主副领域归属。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-P04-6:自动提取阶段只产出 Reference Material、Analysis Artifact、材料关系和 Wiki 编译素材,禁止直接 INSERT/UPSERT 到 entries 表。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-P04-7:用户可在参考资料、Wiki 页面或调研报告中主动标记“沉淀为知识”。系统收到主动标记后,先由 LLM 将素材萃取成能指导决策/行动的 Knowledge Entry 候选,再经过 FR-A07 与 FR-N05;通过后才写入 entries 表。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P04-8:若下游场景需要从领域材料中自动生成 Knowledge Entry,必须声明消费场景和行动价值,并逐条通过统一质量门禁。没有消费场景的学科概念、公式、定理、例题不得自动入库。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。

#### FR-P05:Wiki 准入流程

用户标记调研报告为"采纳"后,系统自动拆解报告内容并生成领域知识条目入库。

- AC-P05-1:用户在调研管理页面(FR-W07)将已完成的调研报告手动标记为"采纳"后,报告进入 Wiki 准入流程。禁止 Agent 自动触发准入,入库权在用户手里。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P05-2:LLM 对报告进行结构化拆解,先生成 Wiki 页面和参考资料。只有报告中能指导未来决策/行动的提炼信息,才生成 Knowledge Entry 候选。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P05-3:生成的 Knowledge Entry 候选经过向量去重检测,与已有知识库条目语义重复的自动合并或丢弃。Wiki 页面和参考资料按来源与主题去重,不进入 Knowledge Entry 去重口径。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P05-4:Knowledge Entry 候选必须通过 FR-A07 与 FR-N05 后入库,归属到语义最匹配的领域知识库(复用 FR-P04 的领域匹配逻辑)。未通过门禁的内容保留为参考资料或 Wiki 页面,不进入 entries 表。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P05-5:Wiki 自身的治理(去重、过期清理、质量审计)由 cron 自动执行,不提供独立 UI。；验收时触发一次计划任务或等效手动运行；日志必须记录开始时间、处理数量、结果状态和失败原因。

#### FR-P06:Wiki 页面聚合生成

从同一主题的参考资料和已通过门禁的 Knowledge Entry 聚合生成可浏览的 Wiki 页面。页面是编译产物,不是手写产物。

- AC-P06-1:系统自动将同主题的 Reference Material 和已通过门禁的 Knowledge Entry 聚合为结构化 Wiki 页面。定义、定理、例题、题目解析等板块来自参考资料;易错点、方法论、决策建议等可来自 Knowledge Entry。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P06-2:页面自动维护双向链接(Knowledge Entry ↔ Reference Material ↔ 相关主题)。这些链接属于 Wiki/材料关系;只有对应对象是 Knowledge Entry 时才同步进入知识图谱。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P06-3:底层 Reference Material 或 Knowledge Entry 更新时页面自动重新生成,保持与底层数据一致。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P06-4:用户浏览入口是 Wiki 页面,底层存储是 Reference Material、材料关系和已通过门禁的 Knowledge Entry。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P06-5:每个页面展示知识来源溯源(从哪个文档的哪一页提取)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P06-6:Wiki 详情页页面结构包含:面包屑导航、标题 + 摘要 + 标签行(类型、来源数、知识点数、关系数、状态)、主体正文(按知识类型组织)、来源材料区、关联知识区、批注区、底部 mini 图谱。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P06-7:来源材料区列出贡献该页面的原始文件,每条显示文件名/页码/段落,可点击「查看原文」跳转到材料详情页。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P06-8:关联知识区展示前置知识、相关参考资料、解题方法、易混点(来自图谱关系)。每条关联标注来源类型(系统推断/同材料共现/用户确认),用户可确认或删除关系。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P06-9:底部 mini 图谱展示当前页面前后 1 跳的关系节点,点击节点跳转到对应 Wiki 页面。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P06-10:操作按钮:「编辑」(修改标题、摘要、正文)、「在图谱中查看」(跳转图谱页聚焦当前节点)、「查看来源材料」(跳转材料详情页)、「添加批注」(选中文字后添加批注)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P06-11:Wiki 页面卡片(列表页)展示:标题、摘要、知识类型标签、来源材料数、知识点数、关系数、状态标记(AI 生成 / 用户已编辑 / 存在争议)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P06-12:Wiki 页面、关联知识区、反向链接和 mini 图谱只展示 active 状态的 Knowledge Entry。Reference Material 可作为来源材料展示,但不得作为已生效知识混入知识列表。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

#### FR-P07:领域知识冲突处理

从不同来源提取的领域知识可能互相矛盾,系统自动识别冲突类型并分别处理。

- AC-P07-1:LLM 自动判断冲突属于三种类型之一:表述差异(同一知识的不同措辞)、新旧更替(旧版本 vs 新版本)、观点分歧(不同学派/视角的合理差异)。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-P07-2:表述差异类冲突,LLM 自动合并为更优表述,保留合并前版本在版本历史中。；验收时使用正例、反例、低置信度样本各 1 组；产出必须包含本 AC 要求的结构化字段和判定理由，不合格样本不得进入下一阶段。
- AC-P07-3:新旧更替类冲突,新知识覆盖旧知识,旧版本保留在版本历史中可一键还原。判断依据:来源文档的出版时间、引用关系、知识的时效性标注。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-P07-4:观点分歧类冲突,两条知识并存,各自标注来源和学派/视角标签,用户浏览时能看到同一主题的不同说法。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-P07-5:冲突处理由治理 cron 自动执行,用户不需要手动处理冲突。但用户可以在版本历史中看到合并/覆盖记录,并可手动干预(还原、删除、编辑)。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-P07-6:领域知识库纳入数据治理 cron 的覆盖范围,与意图知识库共享治理调度但冲突策略独立。；验收时触发一次计划任务或等效手动运行；日志必须记录开始时间、处理数量、结果状态和失败原因。

#### FR-P08:领域知识库顶层结构

领域知识库的顶层固定为「通用知识」和「学科知识」二分结构,系统内置不可删除。

- AC-P08-1:系统初始化时自动创建两个顶层分类:「通用知识」和「学科知识」。这两个顶层分类为系统内置,不可删除、不可重命名。；验收时检查数据库/接口返回的状态字段从操作前状态变为预期状态；异常分支必须写入失败原因，不得静默成功。
- AC-P08-2:「通用知识」容纳跨领域的方法论、经验、决策模式等不归属于特定学科的知识。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-P08-3:「学科知识」容纳按学科/领域组织的专业知识,其下的 Subject 节点由用户自由创建和管理。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-P08-4:用户创建的所有 Subject 必须挂在「通用知识」或「学科知识」之下,不允许创建与这两个顶层平级的分类。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-P08-5:Reference Material 和 Knowledge Entry 入库或归档时,系统根据内容语义自动判断归入「通用知识」还是「学科知识」下的对应 Subject。用户可手动调整归属。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-P08-6:前端领域知识库页面(`/wiki`)左侧目录树顶层固定展示「通用知识」和「学科知识」两个折叠区,用户展开后看到各自下属的 Subject 节点树(详见 §5.2 领域知识浏览旅程)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

#### FR-P09:Subject 数据模型与多对多关系

Subject 作为领域知识库的核心组织单元,通过多对多关系表与材料、知识条目、Wiki 页面关联。

- AC-P09-1:数据模型包含 subjects 表(id、name、description、parent_id、level、created_at、updated_at)和三张关系表:material_subjects(material_id, subject_id)、entry_subjects(entry_id, subject_id)、wiki_page_entries(wiki_page_id, entry_id)。；验收时检查持久化记录或接口响应；本 AC 列出的字段必须非空或符合枚举/格式约束，缺任一项视为失败。
- AC-P09-2:material_subjects 记录材料与 Subject 的归属关系,支持一份材料归属多个 Subject(主+副领域)。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-P09-3:entry_subjects 记录知识条目与 Subject 的归属关系,支持一条知识条目归属多个 Subject。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-P09-4:wiki_page_entries 记录 Wiki 页面与知识条目的关联关系,支持一个页面聚合多条条目、一条条目被多个页面引用。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P09-5:删除 Subject 时,关系表中对应记录级联清理,但不删除关联的材料、条目或页面本身(变为未分类)。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。
- AC-P09-6:前端知识条目详情页展示「所属领域」区域,列出该条目归属的所有 Subject(支持多个),每个可点击跳转到对应领域目录页。材料详情页同理展示多领域归属。；验收时打开对应页面并准备有数据、空数据、失败数据三类样本；页面必须显示本 AC 描述的字段，状态切换后 3 秒内刷新且无空白页。

---

## 8. 非功能需求(NFR)

### 8.1 性能

- NFR-5.1:知识检索响应时间 ≤ 2 秒(P95)。
- NFR-5.2:知识提取不阻塞 Agent 主任务执行,异步完成。
- NFR-5.3:规则分发延迟 ≤ 30 秒(从规则变更到订阅者可查询)。
- NFR-5.4:图谱增量更新延迟 ≤ 5 秒(从知识入库到图谱节点/边可查询)。
- NFR-5.5:Workbench 页面首屏加载时间 ≤ 3 秒(P95,标准网络环境)。

### 8.2 可靠性

- NFR-5.6:知识条目写入后不丢失,支持持久化存储。
- NFR-5.7:冲突检测覆盖所有写入路径,不存在绕过冲突检测的入库方式。
- NFR-5.8:调研任务失败不影响知识库已有数据的完整性。
- NFR-5.9:Analysis Artifact 持久化存储,不因管线后续阶段失败而丢失。

### 8.3 可扩展性

- NFR-5.10:知识类型可扩展,新增类型不修改已有提取和存储逻辑。
- NFR-5.11:调研信息源可扩展,新增来源只需实现统一的获取接口。
- NFR-5.12:规则分发机制与具体 Agent 运行时解耦。
- NFR-5.13:图谱关联信号源可扩展,新增信号类型不修改图谱核心计算逻辑。

### 8.4 安全性

- NFR-5.14:知识条目支持访问控制,敏感知识不跨域泄露。
- NFR-5.15:调研任务不访问用户未授权的信息源。
- NFR-5.16:规则分发不包含超出订阅范围的内容。
- NFR-5.17:用户上传的文档在提取完成后不保留原始文件(可配置保留策略)。

### 8.5 OpenClaw 集成约束

- NFR-5.18:KIVO 核心逻辑不硬编码 OpenClaw 的内部路径、凭据或私有 API 细节,通过集成接口层对接。
- NFR-5.19:更换底层存储或检索引擎后,上层接口行为不变。
- NFR-5.20:OpenClaw 集成接口稳定,OpenClaw 升级不要求 KIVO 核心同步修改。

### 8.6 Web 工作台

- NFR-5.21:Web 工作台所有内部链接使用 basePath 前缀,不产生路径错误导致的 404。
- NFR-5.22:空状态页面提供功能说明和引导操作,不展示纯空白。
- NFR-5.23:图谱可视化在 1000 节点规模下保持流畅交互(≥ 30fps)。
- NFR-5.24:搜索结果返回 < 300ms(P95),列表筛选切换 < 200ms,图谱节点点击到详情面板 < 200ms。
- NFR-5.25:所有写操作 < 100ms 出现 loading 态,操作完成反馈 < 2 秒。
- NFR-5.26:新用户首次体验引导 ≤ 5 分钟完成,引导步骤 ≤ 5 步。
- NFR-5.27:所有状态值和类型值本地化展示,不暴露英文枚举原始值。

### 8.7 NFR 验收标准

- AC-5.1:知识检索在 1000 条知识库规模下满足性能指标。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-5.2:模拟冲突场景下,冲突检测无遗漏。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-5.3:更换底层存储或检索引擎后,上层接口行为不变。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-5.4:审计日志能追溯任一知识条目的完整生命周期。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-5.5:图谱可视化在 1000 节点、3000 边规模下交互流畅。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-5.6:搜索和筛选操作在 1000 条知识库规模下满足 NFR-5.24 延迟指标。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-5.7:新用户按引导流程操作,5 分钟内完成首条知识入库并检索命中。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。
- AC-5.8:在无 Ollama、无 BGE、无网络的环境下执行 `vitest run`,全部测试用例通过且无超时。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-5.9:RelevanceScorer、ContextInjector 等涉及 embedding 的测试,均通过注入 MockEmbeddingProvider 隔离外部依赖。；验收时用一组语义相近与不相近样本验证结果；相关度字段来自向量余弦相似度，禁用关键词回退后仍能按预期通过或明确报错。

### 8.8 语义理解技术约束(全局强制)

适用于 KIVO 全部模块,无例外。

- NFR-5.28:禁止使用 FTS5 MATCH、关键词匹配、正则表达式做知识检索、语义理解、相关性评分、去重判断。适用场景包括但不限于:知识检索、相关性评分、语义去重、词典搜索、域目标匹配、意图匹配、注入筛选。
- NFR-5.29:所有涉及语义理解的场景必须使用向量余弦相似度(embedding cosine similarity)。Embedding provider 是系统强制依赖,不存在「embedding 不可用时降级为关键词」的合法路径。provider 不可用时系统报错并引导修复,不降级。
- NFR-5.30:FTS5 表可保留用于精确文本定位(如按 ID 查找、按术语名精确匹配),但禁止用于语义检索、相关性排序或去重判断。
- NFR-5.31:禁止在代码中实现任何形式的「向量不可用时降级为关键词/FTS5」逻辑。向量检索是唯一合法的语义理解路径。Embedding provider 不可用时,系统必须抛出明确错误并附带修复引导(provider 配置方法、服务启动命令、模型拉取命令),禁止静默降级或 graceful skip。

### 8.9 部署态架构约束(全局强制)

适用于 KIVO 所有 OpenClaw 集成组件(hook、cron、CLI wrapper),无例外。

- NFR-5.32:所有 OpenClaw 集成组件必须是薄壳(thin shell)--只做事件监听、参数传递和结果路由,核心逻辑必须调用 KIVO 包暴露的公开 API。禁止集成组件内维护独立的业务逻辑实现。
- NFR-5.33:搜索逻辑、提取逻辑、治理逻辑、注入逻辑在 KIVO 包内只维护一份实现。所有消费方(hook/cron/CLI/Web API)通过 import 包 API 使用,禁止复制代码或重新实现。
- NFR-5.34:`kivo init` 生成的集成文件通过模块引用(`require`/`import`)消费包能力,用户升级包版本后集成文件自动使用新逻辑,无需手动同步。
- NFR-5.35:KIVO 包的公开 API 遵循语义化版本(SemVer),minor/patch 版本保持向后兼容,破坏性变更仅在 major bump 时允许且必须提供迁移指引。

### 8.10 测试独立性(全局强制)

适用于 KIVO 全部自动化测试,无例外。

- NFR-5.36:所有单元测试和集成测试必须在无外部服务(Ollama、BGE、向量数据库等)的环境下通过。测试不得依赖网络连接或本地运行的第三方服务。
- NFR-5.37:涉及外部服务调用的模块,其构造函数或工厂方法必须支持依赖注入。测试通过注入 mock 实现来隔离外部依赖,禁止在测试中触发真实网络请求。
- NFR-5.38:当模块构造函数提供默认实现(如默认连接 localhost 的 embedding provider),测试必须显式注入 mock,不得依赖默认实现的可用性。
- NFR-5.39:测试套件在 CI 环境(无 GPU、无 Ollama、无外部 API)下全量通过,单个测试用例执行时间 ≤ 5 秒。超时即视为外部依赖泄漏。

---

## 9. 概念架构

### 9.1 系统管什么

KIVO 管理知识的完整生命周期:采集 → 分析 → 结构化 → 存储 → 关联 → 检索 → 注入 → 迭代 → 调研 → 回流。

两个消费面:

- Agent 消费面:通过 API 检索知识、注入上下文、订阅规则。
- 用户消费面:通过 Workbench 导入知识、探索图谱、管理调研、治理知识资产。

### 9.2 管线流转

知识管线(核心路径):
```plaintext
信息来源(对话 / 文档 / 规则文件 / URL 抓取 / 手动录入 / 调研结果)
  → 分析中间产物生成(域 L:提取候选实体、主题、关联、冲突、缺口建议)
  → 知识条目生成(基于分析产物,按知识类型分类)
  → 价值门禁(域 N:评估是否填补 LLM 能力盲区,拦截低价值知识)
  → 冲突检测(域 C:与已有知识比对,判定语义矛盾)
  → 冲突解决(自动策略)
  → 合并检测(域 C:无冲突的同主题知识,判定语义互补可合并)
  → 知识入库(持久化、建立关联、更新图谱)
  → 缺口检测(域 D:分析盲区,结合图谱洞察,产出缺口报告供用户参考)



```

调研管理管线(用户驱动):
```plaintext
用户通过对话发起调研任务 → Agent 执行调研 → 产出报告(md + 飞书文档)
  → 用户查看报告 → 标记"采纳" → Wiki 准入流程(LLM 拆解 → 去重 → 入库)



```

规则分发管线(独立于知识管线):
```plaintext
规则变更检测 → 规则条目更新 → 订阅关系匹配 → 按需推送 → 分发确认



```

意图知识沉淀管线(感知侧):
```plaintext
对话结束事件 → 意图信号检测(LLM 语义识别五类信号)
  → 结构化知识提取(标题 / 正文 / 正例 / 负例)
  → 标准知识管线(冲突检测 → 合并检测 → 入库)
  → 持久化向量索引更新
  → 批量补漏(定时扫描历史对话日志,处理遗漏片段)



```

意图增强管线(消费侧):
```plaintext
Agent 构建 prompt 前 → 向量匹配相关意图知识 → 价值判定(过滤通用常识,只保留填补 LLM 能力盲区的知识)→ 术语注入 → 上下文注入(token 预算内)→ 意图消歧 → 增强后的请求交给 Agent 处理
派发子任务时 → 向量匹配相关意图知识 → 追加到任务 prompt



```

图谱洞察管线(分析侧):
```plaintext
知识入库事件 → 图谱增量更新 → 定期洞察分析(孤立节点 / 桥接节点 / 稀疏社区 / 意外关联)→ 洞察结果写入缺口报告,供用户参考决策是否发起调研



```

存量治理管线(定时触发):
```plaintext
cron 定时触发(默认每周)
  → 过时检测(LLM 评估事实状态是否仍成立)
  → 价值衰减(最后命中时间超过阈值 → 标记低活跃 → 超衰减周期物理删除)
  → 矛盾扫描(BGE 向量预筛 + LLM 精判,发现矛盾对 → 自动冲突解决)
  → 常识回收(LLM 重新评估是否已被通用能力覆盖 → 物理删除)
  → 治理报告输出 + 结果持久化



```

### 9.3 系统边界

- KIVO 管理知识的提取、存储、迭代、检索、图谱、分发和工作台。
- KIVO 不负责 Agent 的任务执行和流程编排(SEVO 的职责)。
- KIVO 不负责 Agent 效果度量和漂移诊断(AEO 的职责)。
- KIVO 不负责设计产物生成(Claw Design 的职责)。
- KIVO 提供知识检索和规则分发接口,OpenClaw 负责在合适的时机调用。
- KIVO 的调研能力依赖 OpenClaw 提供的信息获取工具,KIVO 定义调研任务,OpenClaw 负责执行。

### 9.4 进化机制

- 提取回路:根据误提取、漏提取和用户纠偏记录提升提取质量。
- 检索回路:根据查询未命中和用户反馈优化检索策略。
- 调研回路:根据调研结果质量和用户反馈优化调研执行策略。
- 规则回路:根据分发失败和规则冲突记录优化规则管理。
- 图谱回路:根据洞察准确度和用户对洞察建议的采纳率优化图谱分析算法。

---

## 10. UX 验收标准

UX 验收标准分为体验基线、差异化标准和体验红线,用于判断 KIVO 是否达到陌生用户可用的产品体验。

### 10.1 体验基线

竞品已建立的用户预期,KIVO 必须达到。

<lark-table rows="10" cols="4" header-row="true" column-widths="146,146,146,146">

  <lark-tr>
    <lark-td>
      #
    </lark-td>
    <lark-td>
      标准
    </lark-td>
    <lark-td>
      对标
    </lark-td>
    <lark-td>
      可验证指标
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      B01
    </lark-td>
    <lark-td>
      首次体验引导
    </lark-td>
    <lark-td>
      Notion
    </lark-td>
    <lark-td>
      新用户从注册到看到首条 AI 生成知识 ≤ 5 分钟,引导步骤 ≤ 5 步
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      B02
    </lark-td>
    <lark-td>
      核心操作响应
    </lark-td>
    <lark-td>
      Obsidian
    </lark-td>
    <lark-td>
      搜索返回 < 300ms;列表筛选切换 < 200ms;图谱节点点击到详情面板 < 200ms
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      B03
    </lark-td>
    <lark-td>
      图谱基础交互
    </lark-td>
    <lark-td>
      Obsidian
    </lark-td>
    <lark-td>
      缩放、平移、拖拽、单击高亮邻居、局部图谱(当前节点 N 跳子图)
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      B04
    </lark-td>
    <lark-td>
      图谱性能
    </lark-td>
    <lark-td>
      Obsidian
    </lark-td>
    <lark-td>
      500 节点 ≥ 30fps;1000 节点可用(允许降级渲染)
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      B05
    </lark-td>
    <lark-td>
      搜索基础体验
    </lark-td>
    <lark-td>
      Obsidian
    </lark-td>
    <lark-td>
      关键词高亮、结果排序、类型/状态过滤、键盘上下导航、最近搜索历史
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      B06
    </lark-td>
    <lark-td>
      空状态处理
    </lark-td>
    <lark-td>
      Notion
    </lark-td>
    <lark-td>
      所有页面空状态有明确下一步引导,无空白页
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      B07
    </lark-td>
    <lark-td>
      数据操作反馈
    </lark-td>
    <lark-td>
      Notion
    </lark-td>
    <lark-td>
      所有写操作 < 100ms 出现 loading 态,完成后有成功提示
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      B08
    </lark-td>
    <lark-td>
      表格视图
    </lark-td>
    <lark-td>
      Notion
    </lark-td>
    <lark-td>
      知识列表使用表格视图展示,支持排序和筛选
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      B09
    </lark-td>
    <lark-td>
      版本差异对比
    </lark-td>
    <lark-td>
      通用
    </lark-td>
    <lark-td>
      知识条目版本历史支持逐字段 diff
    </lark-td>
  </lark-tr>
</lark-table>

### 10.2 差异化标准

竞品做不到或做得不好的领域,KIVO 凭 AI 驱动架构应超越。

<lark-table rows="8" cols="4" header-row="true" column-widths="146,146,146,146">

  <lark-tr>
    <lark-td>
      #
    </lark-td>
    <lark-td>
      标准
    </lark-td>
    <lark-td>
      竞品短板
    </lark-td>
    <lark-td>
      KIVO 差异化指标
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      S01
    </lark-td>
    <lark-td>
      AI 关系发现
    </lark-td>
    <lark-td>
      Obsidian 手动链接,Notion 无图谱,Heptabase 手动连线
    </lark-td>
    <lark-td>
      自动发现知识间关系并推荐,用户确认即建链;推荐确认率 > 70%
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      S02
    </lark-td>
    <lark-td>
      语义搜索
    </lark-td>
    <lark-td>
      三款竞品均无原生语义搜索
    </lark-td>
    <lark-td>
      自然语言查询,结果按语义相关度排序
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      S03
    </lark-td>
    <lark-td>
      图谱可操作性
    </lark-td>
    <lark-td>
      Obsidian 图谱只读,Heptabase 需手动
    </lark-td>
    <lark-td>
      图谱上直接创建知识、建立关系、发起调研、标记冲突
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      S04
    </lark-td>
    <lark-td>
      知识进化可视化
    </lark-td>
    <lark-td>
      三款竞品均无时间维度
    </lark-td>
    <lark-td>
      时间轴回放:滑动时间条看知识网络生长,识别爆发期和停滞期
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      S06
    </lark-td>
    <lark-td>
      调研管理闭环
    </lark-td>
    <lark-td>
      无竞品有此功能
    </lark-td>
    <lark-td>
      用户发起调研 → Agent 执行 → 产出报告 → 标记采纳 → Wiki 入库,全链路可视化
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      S07
    </lark-td>
    <lark-td>
      信息密度适配
    </lark-td>
    <lark-td>
      Obsidian 信息过载,Notion 结构过重
    </lark-td>
    <lark-td>
      页面信息密度合理,不过载不空洞
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      S08
    </lark-td>
    <lark-td>
      首次价值展示
    </lark-td>
    <lark-td>
      Obsidian 需大量手动输入
    </lark-td>
    <lark-td>
      导入首份文档后 30 秒内自动提取知识、建立关系、生成图谱
    </lark-td>
  </lark-tr>
</lark-table>

### 10.3 体验红线

绝对不能出现的体验问题。

<lark-table rows="7" cols="3" header-row="true" column-widths="244,244,244">

  <lark-tr>
    <lark-td>
      #
    </lark-td>
    <lark-td>
      红线
    </lark-td>
    <lark-td>
      原因
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      R01
    </lark-td>
    <lark-td>
      任何页面出现未处理的空白状态
    </lark-td>
    <lark-td>
      Notion 已将空状态引导做成行业标准
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      R02
    </lark-td>
    <lark-td>
      搜索延迟 > 1 秒
    </lark-td>
    <lark-td>
      Obsidian 用户习惯 < 200ms,云端产品底线 1 秒
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      R03
    </lark-td>
    <lark-td>
      图谱渲染卡顿到无法交互
    </lark-td>
    <lark-td>
      图谱是门面功能
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      R04
    </lark-td>
    <lark-td>
      AI 操作无法撤销
    </lark-td>
    <lark-td>
      AI 自动建立的关系和提取的知识必须可一键撤销
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      R05
    </lark-td>
    <lark-td>
      操作后无反馈超过 2 秒
    </lark-td>
    <lark-td>
      用户会以为系统挂了
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      R06
    </lark-td>
    <lark-td>
      英文原始值暴露在 UI 上
    </lark-td>
    <lark-td>
      fact/active/high 等必须本地化展示
    </lark-td>
  </lark-tr>
</lark-table>

---

## 11. 与 Self-Evolving Harness 其他模块的边界

### 11.1 与 SEVO 的边界

SEVO 负责研发流水线的阶段推进和交付闭环。KIVO 为 SEVO 提供知识支撑。

- KIVO 提供:历史规格、方法论、规则、经验、意图线索。
- SEVO 消费:把这些知识用于 Spec 编写、Contract 设计和 Review 参考。
- 边界结论:KIVO 管"知道什么",SEVO 管"如何把一次研发任务做完"。
- Research Task(KIVO)vs Pipeline Task(SEVO):前者由用户发起的知识调研,后者完成研发交付,属于不同层面。

### 11.2 与 AEO 的边界

AEO 负责度量 Agent 效果、发现漂移、诊断根因。KIVO 负责知识资产本身的治理。

- 交互点:AEO 发现效果漂移后,可触发 KIVO 的缺口检测,排查是否因知识缺失导致。
- 边界结论:AEO 管"做得怎么样",KIVO 管"知道什么"。

### 11.3 与 OpenClaw 宿主环境的边界

- OpenClaw 负责:Agent 运行、工具接入、消息调度、执行沙箱。
- KIVO 负责:知识提取逻辑、存储结构、检索算法、冲突解决策略、规则分发协议、图谱计算、工作台 UI。
- 边界结论:KIVO 定义知识管理语义,OpenClaw 做运行时集成。

---

## 12. 约束与假设

### 12.1 约束

- KIVO 只服务于 OpenClaw 宿主环境,不承诺支持其他平台;但代码不硬编码 OpenClaw 内部路径和私有实现细节,保持工程质量。
- 知识条目的存储格式和检索接口与具体向量数据库解耦。
- 调研任务的执行依赖 OpenClaw 提供的工具能力,KIVO 只定义任务不直接执行。
- 规则分发采用拉取优先策略,推送为补充。
- 图谱计算在本地完成,不依赖外部图数据库(初期)。
- **Hook 薄壳与单一实现约束**:所有 OpenClaw 集成组件(Gateway hook、cron 脚本、CLI wrapper)必须是薄壳,核心逻辑统一由 KIVO 包 API 提供。禁止集成组件内维护独立的搜索、提取、治理实现,确保用户升级包版本后所有运行态组件自动使用新逻辑。具体要求:
  - Gateway hook 只做事件监听、参数解析和结果传递,核心逻辑必须通过 `require('@self-evolving-harness/kivo')` 调用包 API。
  - cron 脚本只做定时触发和参数组装,实际执行逻辑必须调用 KIVO 包的 CLI 命令或编程 API。
  - `kivo init` 生成的 hook 通过 require 引用包模块,`npm update` 后自动使用新版逻辑。
  - KIVO 包公开 API 保持向后兼容,破坏性变更只允许在 major version bump 时发生。
  - hook/cron/CLI 只允许 import `package.json` exports 声明的公开入口,禁止引用包内部路径。
  - 代码审查必须检查 hook/cron/script 中是否存在内联实现,发现则视为 P0 阻断项。
  - `kivo doctor --hooks` 检测已部署 hook 是否引用当前包版本 API 及是否存在内联实现。

### 12.2 假设

- OpenClaw 环境能提供基本的文件读写、语义搜索和网络访问能力。
- Agent 对话记录可被 KIVO 访问用于知识提取。
- 用户接受 Agent 在后台执行调研任务消耗一定计算资源。
- 知识库规模在初期控制在万级条目以内。
- 图谱规模在初期控制在千级节点以内。

### 12.3 非目标

- KIVO 不负责 Agent 的任务执行和流程编排(SEVO 的职责)。
- KIVO 不负责 Agent 效果度量和优化建议(AEO 的职责)。
- KIVO 不负责设计产物生成(Claw Design 的职责)。
- KIVO 不替代通用搜索引擎,调研能力服务于知识缺口填补。
- KIVO 不替代 Obsidian 等通用笔记工具,聚焦结构化知识管理和 Agent 知识消费。

### 12.4 总体验收标准

- AC-12-1:文档完整覆盖产品定位、用户、15 域 FR、NFR、概念架构、模块边界和约束。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-12-2:文档内容不依赖 OpenClaw 某个特定版本的私有实现才能成立。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-12-3:读者可以据此进入后续 Spec Review 和架构设计,不需要额外口头补课。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。
- AC-12-4:文档正文不含修订痕迹、过程噪音和 AI 套话。；验收时通过一次成功路径和一次边界路径验证；操作后可观测数据、状态或日志必须与本 AC 描述一致。

