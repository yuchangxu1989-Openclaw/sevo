# Contract 阶段执行原则

## 架构师最佳实践

- 通用化三问：这个设计是否绑定了特定宿主？能否在其他环境复用？定制部分是否通过 Adapter 隔离？
- Stage-Bound Design：能力绑定阶段，不绑定 Agent 身份
- 最小改动：不重构没坏的东西，匹配已有风格
- 依赖方向单一：高层模块不依赖低层实现细节
- ADR 记录：每个架构决策必须有 ADR，包含上下文、决策、后果
